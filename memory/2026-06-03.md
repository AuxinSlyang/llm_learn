# 2026-06-03

- 今日完成：DPO / Self-Consistency / ReAct 三篇 quick read，并将核心 takeaway 写入对应 `QUICK_READ.md` 与 Daily Note。
- 今日关键理解：DPO 是 reference-anchored pairwise log-prob loss；Self-Consistency 是多条 CoT 路径采样后的 final answer voting；ReAct 是 `Thought -> Action -> Observation` 的 prompt/runtime agent loop。
- 今日未完成：tokenizer 视频后半段与 `nanogpt-mainline-summary-v0.md` 的 tokenizer 接口说明未现场收口。
- 明天主线：先完整收束 tokenizer，写清 `text -> UTF-8 bytes -> BPE merge -> token ids -> batch -> embedding/logits`，再 quick scan 2-3 篇核心论文。
- 明天论文建议：优先 `Toolformer` + `RAG`；若 tokenizer 已完成且仍有精力，再从 `RoPE / ALiBi` 中选一篇，不展开长上下文专题。
