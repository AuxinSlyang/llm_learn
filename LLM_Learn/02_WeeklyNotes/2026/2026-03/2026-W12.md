---
type: weekly_note
week: 2026-W12
annual_plan: [[LLM_Infra_Annual_Plan_2026]]
annual_plan_week: 2026-03 / Week 3
time_budget: 14-16h
track: LLM Inference / Serving
gpu_env: V100
---

# Week 2026-W12 学习记录

## 本周锚点

- 年度计划周次：`2026-03 / Week 3`
- 日期范围：`2026-03-16 ~ 2026-03-22`
- 本周主题：`先推进并基本看完 makemore 第一集，用最小输出把训练闭环钉住`
- 本周硬产出：`makemore 第一集阶段性笔记` + `Bigram / logits / cross entropy 最小解释` + `W13 入口`

## 本周和转岗目标的关系

这周不追求“马上进入更复杂 serving 细节”，而是先把后面所有内容都要反复用到的地基补扎实：

- 语言模型最小工作方式：`token -> embedding -> logits -> loss -> generate`
- 神经网络训练最小闭环：`forward -> loss -> backward -> optimizer.step`
- Transformer/LLM 最小主流程：`embedding -> attention -> logits -> decode`

如果这周做扎实，后面再看 `KV cache / TTFT / TPOT / vLLM / SGLang / 调度`，你会更容易把工程问题映射回模型计算本身。

## 本周唯一主线

- [ ] 推进并尽量看完 `makemore` 第一集，先把 `Bigram -> logits -> cross entropy -> backward -> generate` 这条最小主线讲清

## 本周最低完成线（先保底）

- [ ] 把 `makemore` 从开头推进到第一集主体后段，最好完成第一集
- [ ] 至少留 `6-10` 条自己的笔记，先覆盖 `Bigram / logits / cross entropy`
- [ ] 能讲清 `Bigram / logits / cross entropy`，并起步覆盖 `backward / optimizer.step / generate`
- [ ] 写出一版 `Bigram -> logits -> cross entropy` 的 `5-8` 句解释
- [ ] 给 `W13` 留下 `2-3` 个明确入口任务

## W11 未完成项顺延到 W12

> 原则：全部纳入 W12 视野，但分优先级执行；先补“认知地基”，再补“系统概念”，最后才补“实验与文档”。

### P0：本周必须完成
- [ ] `makemore`：继续推进第一集，并尽量完成第一集主体
- [ ] 笔记：写清 `Bigram / logits / cross entropy`
- [ ] 笔记：起一版 `训练闭环 5-8 句版本`
- [ ] 收口：给 `W13` 留下 `2-3` 个明确入口任务

### P1：本周尽量完成
- [ ] 起一版 `训练 vs 推理`
- [ ] 起一版 `最小 LLM 主流程`
- [ ] 产出一页 `Week 3 学习总结`
- [ ] 如果有余力，再补 `backward / optimizer.step / generate`

### P2：有额外时间再做
- [ ] 固定 `2` 个 prompt（短问答 + 200-400 字总结）
- [ ] 补 `1` 次生成耗时记录
- [ ] 补 `1` 次生成前后 `nvidia-smi` 显存记录
- [ ] 写 `最小本地推理记录`
- [ ] 写 `V100 环境说明文档`

## 本周目标产物

- [ ] 笔记 1：`makemore 第一集阶段性笔记`
- [ ] 笔记 2：`Bigram / logits / cross entropy` 最小解释
- [ ] 笔记 3：`训练闭环 5-8 句版本`
- [ ] 笔记 4：`W13 入口任务`
- [ ] 清单 1：`W11 顺延项完成/未完成/顺延原因`

## 时间预算（13.5-15.5h）

- [ ] 周一 2h：`makemore` 开篇 + 训练闭环
- [ ] 周二 2h：`makemore` 中段 + `embedding / logits / cross entropy`
- [ ] 周三 2h：`makemore` 继续 + `backward / optimizer.step / generate`
- [ ] 周四 2h：`attention` 最小链路 + `训练 vs 推理`
- [ ] 周五 1-1.5h：经典阅读收口 + `LLM / Agents / Scaling Law` 回接
- [ ] 周六：休息，不安排正式学习任务
- [ ] 周日 2h：继续推进 `makemore` 第一集，整理本周最小笔记，并写周复盘和 W13 入口

## 03-17 / 03-18 现实重排

> 现实情况：`2026-03-17` 晚上主要完成了 `1` 篇文档阅读，但因加班未能执行既定 `makemore` 输出任务；`2026-03-18` 是活动日，预计可在 `18:30` 后拿到一段完整晚间时间。调整原则是不补所有欠账，只拼成一个约 `4h` 的完整学习块。

### 合并后的最低完成线
- [ ] 看完 `makemore` 当前主线核心段落
- [ ] 写清 `Bigram / logits / cross entropy / loss.backward() / optimizer.step()`
- [ ] 写出 `训练闭环 5-8` 句版本
- [ ] 起一版 `训练 vs 推理` 草稿

### 合并后的建议顺序
- [ ] 第 1 小时：回接 03-17 文档输入，完成 `Bigram -> logits -> loss`
- [ ] 第 2 小时：写 `Bigram / logits / cross entropy`
- [ ] 第 3 小时：写 `backward / optimizer.step / generate`
- [ ] 第 4 小时：收口成 `训练闭环 + 训练 vs 推理` 草稿

### 明确不做
- [ ] 不开 `KV cache / metrics / profiling`
- [ ] 不补环境、脚本、benchmark
- [ ] 不追求把 `makemore` 一口气看很远，只收当前主线

## 按你的作息重排（日计划梗概）

> 工作日尽量保持 `45-60m 视频输入 + 15-20m 摘记 + 30-45m 输出/整理`；周末用于长时间连续观看和收口。

### 周一（约 2h）
- 视频：`makemore` 前 60m，先抓 `token / embedding / logits / next token`
- 整理：写 `embedding` 和 `logits` 各 `2-3` 句自己的话
- 输出：写 `forward -> loss -> backward -> step` 最小闭环

### 周二（约 2h）
- 视频：`makemore` 再看 60-75m，重点是 `cross entropy / gradients`
- 整理：回答两个问题：
  - `cross entropy` 在做什么
  - `loss.backward()` 在做什么
- 输出：把 `训练循环` 压缩成 `5-8` 句自己的话

### 周三（约 2h）
- 视频：`makemore` 再推进 60-75m，重点看 `generate`
- 整理：回答两个问题：
  - `optimizer.step()` 在做什么
  - `generate` 为什么只取最后一个时间步
- 输出：完成一页 `训练 vs 推理`

### 周四（约 2h）
- 视频：继续 `makemore`，先不强求进入太多 `attention / Transformer`
- 阅读：只在有余力时回看已有笔记，不新开经典阅读
- 输出：优先把 `Bigram / logits / cross entropy` 讲清

### 周五（约 1-1.5h）
- 经典阅读：本周默认不作为必须项，统一顺延到 `W13`
- 输出：如果时间够，补 `训练闭环 5-8 句版本`

### 周六
- 休息：不安排正式学习任务
- 可选：如果脑子里冒出问题，只记一句，不展开

### 周日（约 2h）
- 收口：
  - 继续推进 `makemore` 第一集，尽量完成第一集
  - 完成 `makemore` 第一集阶段性笔记
  - 写出 `Bigram -> logits -> cross entropy` 最小解释
  - 给 `W13` 留下 `2-3` 个入口任务
- 复盘：
  - 写 `W11 顺延项完成/未完成/原因`
  - 写下本周真实阻塞和下周降难方案

## 本周输入材料

- [ ] 视频：Karpathy `makemore` 全流程（本周主输入，约 `10h`）
- [ ] 文章：`The Bitter Lesson` by Rich Sutton（本周非必须，顺延优先）
- [ ] 论文：`Attention Is All You Need`（本周非必须，顺延优先）
- [ ] 视频：`04 (Aside) - LLM, Agents 和 Scaling Law [2026 南京大学操作系统原理]`（本周非必须）
- [ ] 可选回看：你本周已有的 `makemore / Bitter Lesson / LLM lecture` 笔记

## 本周任务清单

### 理论与概念（P0）
- [ ] `Bigram` 是什么
- [ ] `embedding` 是什么
- [ ] `logits` 是什么
- [ ] `cross entropy` 在做什么
- [ ] `loss.backward()` 在做什么
- [ ] `optimizer.step()` 在做什么
- [ ] `generate` 为什么只取最后一个时间步
- [ ] `attention` 在做什么（顺延到 W13 也可）
- [ ] `Q / K / V` 分别是什么（顺延到 W13 也可）

### 阅读与理解（P0/P1）
- [ ] 从 `The Bitter Lesson` 提炼 `3` 条对 LLM 的启发（顺延优先）
- [ ] 从 `Attention Is All You Need` 提炼 `3` 条最关键机制（顺延优先）
- [ ] 从 `LLM, Agents 和 Scaling Law` 提炼 `3` 条世界观结论（顺延优先）

### 系统回接（P1）
- [ ] 写 `prefill` 一句话定义
- [ ] 写 `decode` 一句话定义
- [ ] 写 `KV cache` 一句话定义
- [ ] 写清 `TTFT` 主要对应哪里
- [ ] 写清 `TPOT` 主要对应哪里

### 实验与文档（P2）
- [ ] 固定 `2` 个 prompt
- [ ] 留 `1` 次耗时记录
- [ ] 留 `1` 次显存记录
- [ ] 写 `最小本地推理记录`
- [ ] 写 `V100 环境说明文档`

### 笔记与输出（P0）
- [ ] 写 `makemore` 第一集阶段性笔记
- [ ] 写 `Bigram -> logits -> cross entropy` 最小解释
- [ ] 写 `训练闭环 5-8 句版本`
- [ ] 写 `W13` 入口任务
- [ ] 写 `Week 3 学习总结`

## 本周实验设计（最小版）

- 模型：`先不新增模型主线`
- 框架：`PyTorch / makemore 代码阅读为主`
- GPU：`V100`
- Workload：`本周以理解最小训练/生成闭环为主，不强推 benchmark`
- 指标：`理解正确性 > 记录数据量`
- 对照组：`bigram / attention 最小差异`
- 预期观察：`一旦理解最小链路，后面看 serving 指标会更顺`

## 本周源码关注点

- 入口模块：`makemore` 中数据流从输入 token 到 logits 的路径
- 训练模块：`loss.backward()` 和 `optimizer.step()`
- 生成模块：`generate` 如何循环地产生下一个 token
- Transformer 模块：`Q / K / V -> attention -> output`
- 暂时不懂的点：先记下来，不在本周一口气深挖全部公式

## 本周交付物

- [ ] 笔记：`makemore` 第一集阶段性笔记
- [ ] 笔记：`Bigram -> logits -> cross entropy` 最小解释
- [ ] 笔记：`训练闭环 5-8 句版本`
- [ ] 小结：`W11 顺延项处理结果`
- [ ] 小结：`W13 入口 2-3 条`

## 风险与阻塞

- 当前最大阻塞：`makemore` 总时长远超本周剩余可用时间，而当前实际进度只到第一集前 30m。
- 需要缩小范围的点：本周先不把主精力放在 `benchmark / profiling / 环境折腾`。
- 降难策略：
  - 不再以“看完整个 `makemore`”作为 W12 目标，改为“尽量完成第一集并留下最小输出”
  - 如果 `makemore` 仍看不完，至少保住 `Bigram / logits / cross entropy` 这条最小主线
  - 如果 `Attention Is All You Need` 太硬，整项顺延到 `W13`
  - 如果工作继续打断，P2 全部顺延，不和 P0 抢时间

## 本周执行原则

- [ ] 以 `makemore` 为主线，不并行开太多新主题
- [ ] 经典阅读只做“辅助理解”，不追求一次看完全部论文
- [ ] 先能用自己的话讲清，再追公式和代码细节
- [ ] 每次输入后都留 `3-5` 句自己的总结，不只留链接

## 未来 12 周路线图入口

- 总路线图：[[2026-W12_to_W23_12周路线图]]
- 具身智能年度路线图：[[Embodied_AI_Infra_Annual_Plan_2026]]
- 当前定位：
  - `W12-W13`：`makemore` 吃透，补齐模型地基
  - `W14-W15`：补 `prefill / decode / KV cache / TTFT / TPOT`
  - `W16-W18`：系统进入 `CS336`
  - `W19-W21`：最小实现 + `vLLM / SGLang`
  - `W22-W23`：专项深化
- 后续主攻方向预设：
  - `scheduler`
  - `KV cache / memory management`
  - `tail latency / observability`

## 本周复盘

- 本周最重要的收获：`开始真正进入 makemore 主线，并把 Bigram -> 概率分布 -> 采样生成 -> MLE 这条最小统计建模链路串起来了`
- 本周最真实的瓶颈：`上周工作过忙，完整时间块明显不足，导致最初把“看完整个 makemore”当成 W12 目标本身就不现实`
- 本周最有价值的证据：`已经留下 1 份项目笔记：03_Projects/8. makemore/01. Bigram_统计建模与采样生成/makemore-1-firsthalf_2026-03-22.md，并且对 Bigram / 条件概率 / 采样 / log likelihood 有了第一版自己的理解`
- 哪些事情不值得继续投入：`在时间块不完整的一周里，同时追 makemore 全量、经典阅读、attention 图和额外系统概念，这种排法不值得继续`
- 下周唯一主线：`继续以看完 makemore 为唯一主线，优先完成主体观看，再把关键概念压成自己的可复述版本`
- 下周唯一主线：`完成 makemore 第一集收口，并把本周碎笔记压成训练闭环与最小 LLM 主流程的第一版`
