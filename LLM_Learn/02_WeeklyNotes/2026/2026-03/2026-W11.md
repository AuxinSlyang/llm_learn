---
type: weekly_note
week: 2026-W11
annual_plan: [[LLM_Infra_Annual_Plan_2026]]
annual_plan_week: Week 2
time_budget: 12-15h
track: LLM Inference / Serving
gpu_env: V100
---

# Week 2026-W11 学习记录

## 本周锚点

- 年度计划周次：`Week 2`
- 日期范围：`2026-03-09 ~ 2026-03-15`
- 本周主题：`先补 makemore 地基，再回到推理主流程`
- 本周硬产出：`makemore 基础笔记` + `KV Cache/metrics 两页笔记` + `最小本地推理记录` + `V100 环境说明文档`

## 本周和转岗目标的关系

这周不是“多看一点材料”，而是在补你转向 `LLM Inference / Serving` 最关键的第一层能力：

- 能讲清最小深度学习/语言模型机制
- 能讲清训练 vs 推理的边界
- 能把 `KV cache / TTFT / TPOT` 放进系统语境里理解

如果这周完成得扎实，它会直接成为你后面重新进入 `Karpathy / vLLM / SGLang / benchmark / 调度` 的地基。

## 本周最低完成线（先保底）

- [ ] 完成 `makemore` 基础理解：`embedding / logits / cross entropy / backward / generate`
- [ ] 写完一页 `训练 vs 推理` 自己的解释
- [ ] 写完 `prefill / decode / KV cache` 三个一句话定义
- [ ] 写完一段 `TTFT / TPOT / prefill / decode / KV cache` 对照说明
- [ ] 固定 2 个 prompt，并补齐至少 1 组 `response + 耗时 + nvidia-smi`
- [ ] 补完 `最小本地推理记录` 与 `V100 环境说明文档`

## 本周目标产物（对年底转岗有用的证据）

- [ ] 笔记 1：`训练 vs 推理`（至少回答 5 个问题）
- [ ] 笔记 2：`KV cache / prefill / decode`（至少回答 4 个问题）
- [ ] 笔记 3：`TTFT / TPOT / 显存 / 吞吐`（至少回答 4 个问题）
- [ ] 文档：`最小本地推理记录`

## 时间预算（建议执行顺序）

- [ ] 周一 2h：W10 收口任务清点 + Karpathy 前 45-60m
- [ ] 周二 2h：KV Cache 文章 + 三个最小定义
- [ ] 周三 2h：vLLM metrics 或 SGLang profiling 首页 + 指标映射
- [ ] 周四 2h：固定两个 prompt，补 1-2 组推理记录
- [ ] 周五 2h：补文档（最小本地推理记录 + V100 环境说明文档）
- [ ] 周六 2-3h：Attention 最小链路图（Q/K/V -> attention -> output）
- [ ] 周日 1-2h：周复盘 + W12 预热

## 按你的作息重排（日计划梗概）

> 约束：工作日每天固定 `45m 阅读（骑车） + 15m 整理 + 45m coding/实验`；主要动手放周日；周五+周六总学习 `2-3h`。

### 周一（约 1h45m）
- 阅读 45m：Karpathy 前半（只抓 `token/embedding/logits/next token`）
- 整理 15m：把 4 个关键词写成 `3-5` 条摘记
- 动手 45m：把 HF 笔记收口成一页可复述版本（补到周记）

### 周二（约 1h-1h45m）
- 阅读 45m：切到 `makemore`，只补 `embedding / logits / cross entropy / backward / generate`
- 整理 15m：写 `forward / backward / optimizer.step` 的三句区别
- 动手 30-45m：把 `训练循环` 和 `生成流程` 补成一页笔记，不强求进入 KV Cache

### 周三（约 1h45m）
- 阅读 45m：继续 `makemore` 或回看 Karpathy，只补没吃透的基础点
- 整理 15m：写 `embedding / logits / cross entropy` 三句自己的话
- 动手 45m：完成 `训练 vs 推理` 一页总结

### 周四（约 1h45m）
- 阅读 45m：`KV Cache from scratch`（只看 prefill/decode/KV cache）
- 整理 15m：写三个一句话定义
- 动手 45m：整理一次历史实验输出，补 `耗时 + 显存` 记录模板

### 周五（0.5-1h，低负荷）
- 阅读 30-40m：回看本周笔记，补漏 1-2 条
- 整理 10-15m：列周日实验待办
- 动手可选 10-15m：检查脚本可运行性

### 周六（1.5-2h，低负荷）
- 阅读 45-60m：回看 Karpathy/KV/metrics 三页
- 整理 15-20m：收口成周日执行清单
- 轻动手 30-40m：固定周日两个 prompt + 记录表头

### 周日（4-6h，主实验日）
- 动手主线：
  - 跑两个 prompt（短问答 + 200-400 字总结）
  - 记录 response、耗时、生成前后 `nvidia-smi`
  - 补齐 `最小本地推理记录` 与 `V100 环境说明文档`
- 收口 30-45m：写周复盘与 W12 入口 3 条

## W11 后半周重排（按你最新时间预算）

> 时间预算：`03-12 2h`，`03-13 1h（只读）`，`03-14 2h`，`03-15 8h`

### 03-12（2h）
- 主线：先补 `makemore` 地基，再收口 `训练 vs 推理`
- 必做：
  - 回答 5 个问题：
    - `embedding` 是什么
    - `logits` 是什么
    - `cross entropy` 在做什么
    - `loss.backward()` 在做什么
    - `optimizer.step()` 在做什么
  - 写完一页 `训练 vs 推理` 总结
- 完成标准：
  - 能不看资料讲清上面 5 个问题中的至少 4 个
  - 留下至少半页自己的笔记

### 03-13（1h，只读）
- 主线：轻输入，不做 coding
- 必做：
  - 回看 `makemore` 或 Karpathy 中没吃透的部分
  - 只回答两个问题：
    - `cross entropy` 在做什么
    - `generate` 为什么只取最后一个时间步
- 可选（仅当前两项完成后还有精力）：
  - 看 `micrograd` 前半中 `computation graph / chain rule / backward` 的最小部分
  - 目标不是看完，而是回答：`为什么 loss 可以一路把梯度传回参数`
- 完成标准：
  - 每个问题写 2-3 句自己的话
  - 不要求写代码，不切新主题

### 03-14（2h）
- 主线：正式进入 `KV Cache`，并为周日实验做模板
- 必做：
  - 阅读 `KV Cache from scratch` 或 `vLLM metrics`
  - 回答 4 个问题：
    - `prefill` 是什么
    - `decode` 是什么
    - 为什么长上下文更贵
    - `TTFT` 和 `TPOT` 主要对应哪段开销
  - 固定两个 prompt 和记录表头
- 完成标准：
  - 写完一段 `TTFT / TPOT / prefill / decode / KV cache` 对照说明
  - 周日实验模板可直接执行

### 03-15（8h）
- 主线：本周主实验日 + 文档收口
- 必做：
  - 跑两个 prompt
  - 记录 `response / 耗时 / nvidia-smi`
  - 补完 `最小本地推理记录`
  - 补完 `V100 环境说明文档`
  - 写 `W10 遗留补齐情况`
  - 写 `W11 周复盘 + W12 入口 3 条`

## W11 后半周现实版重排（03-12 未执行后）

> 调整原则：不补所有欠账，只保留最小主线；本周先把 `makemore -> 训练 vs 推理` 补起来，`KV cache / 实验 / 文档` 视时间顺延。

### 03-13（45-60m，只读）
- 主线：轻量补输入，不追求产出完整笔记
- 必做：
  - 看 `makemore` 第一集前半，或回看 Karpathy 基础片段
  - 只盯两个问题：
    - `cross entropy` 在做什么
    - `generate` 为什么只看最后一个时间步
- 完成标准：
  - 每个问题写 `2-3` 句自己的话
  - 不切去 `KV cache`
  - 不切去完整 `micrograd`

### 03-14（1.5-2h，主补位）
- 主线：补回 `03-12` 的真正主任务
- 必做：
  - 回答 `5` 个基础问题：
    - `embedding`
    - `logits`
    - `cross entropy`
    - `loss.backward()`
    - `optimizer.step()`
  - 写完一页 `训练 vs 推理`
- 完成标准：
  - 能脱离资料讲清 `5` 个问题中的至少 `4` 个
  - 留下一页自己的可复述笔记

### 03-15（视精力二选一，不全要）
- 方案 A：如果 `03-14` 还没补扎实
  - 继续收口 `训练 vs 推理`
  - 把 `5-8` 句可复述版本写出来
  - `KV cache` 延到 `W12`
- 方案 B：如果 `03-14` 已完成
  - 进入 `KV cache` 最小阅读
  - 固定两个 prompt 和记录表头
  - 只做实验准备，不强求当天跑完整实验

## 03-15 今日临时调整（按实际选择回写）

> 2026-03-15 的现实主线改为：先看一节完整的 LLM lecture，补“整体 taste”，不再强行把今天压成实验日。

- 调整原因：
  - 这周前半没有把基础大图景彻底看顺，直接做实验容易只记录现象，不理解问题边界
  - 你今天更需要的是一节完整 lecture 帮你把 `模型原理 -> 能力来源 -> 工程问题` 串起来
- 今日最小完成线改为：
  - 看完 lecture 主体内容
  - 记 `6-10` 个时间戳要点
  - 写一页 `这节内容和 Inference / Serving 有什么关系`
- 顺延项：
  - `prompt / 耗时 / nvidia-smi`
  - `最小本地推理记录`
  - `V100 环境说明文档`
- 顺延原则：
  - 不把顺延项塞回今天
  - 优先把 lecture 理解压缩成可复述版本，再进入 `W12`

### W12 兜底策略
- 如果 `W11` 后半仍然被打断：
  - `W12` 唯一主线仍是 `makemore 地基 + 训练 vs 推理`
  - `KV cache / metrics / 最小本地推理记录` 整体后移
  - 不因为延期而并行开更多主题

## 本周输入材料

- [ ] Karpathy 视频（前 60-90m）：https://www.youtube.com/watch?v=kCc8FmEb1nY
- [ ] B 站完整 LLM lecture（03-15 临时主输入）：https://www.bilibili.com/video/BV1gEcmzzE4P/
- [ ] KV Cache from scratch in nanoVLM：https://huggingface.co/blog/kv-cache
- [ ] vLLM Metrics：https://docs.vllm.ai/en/stable/design/metrics/
- [ ] SGLang Benchmark & Profiling：https://docs.sglang.io/developer_guide/benchmark_and_profiling.html
- [ ] Transformer 论文（只读 attention 核心）：https://arxiv.org/abs/1706.03762

## 本周任务清单

### A. W10 遗留收口（优先级 P0）
- [ ] 把 `HF Text generation + chat template` 笔记压缩成 8-12 行可复述版本
- [ ] 固定 2 个 prompt（短问答 + 200-400 字总结）
- [ ] 至少补 1 次生成前后 `nvidia-smi` 显存记录
- [ ] 记录 1 次生成耗时（起止时间或脚本计时）
- [ ] 写完 `最小本地推理记录`
- [ ] 写完 `V100 环境说明文档`

### B. Week 2 主线（优先级 P1）
- [ ] makemore：补齐 `embedding / logits / cross entropy / backward / generate`
- [ ] KV Cache：回答 `prefill 是什么 / decode 是什么 / 为什么长上下文更贵`
- [ ] 指标映射：写清 `TTFT` 主要对应哪里，`TPOT` 主要对应哪里
- [ ] 画一张最小链路图：`prompt -> tokenize -> prefill -> decode -> text`

### C. 输出与复盘（优先级 P1）
- [ ] 产出一页 `Week 2 输入日总结`（不超过 1 页）
- [ ] 产出一页 `W10 遗留补齐情况`（完成/未完成/原因）
- [ ] 给 W12 留 3 个明确入口任务（不超过 3 条）

## 本周实验设计（最小版）

- 模型：`Qwen2.5-1.5B-Instruct`（主）/ `Mistral-7B-v0.1`（对照）
- 框架：`PyTorch + transformers`
- GPU：`V100`
- Workload：
  - `prompt-1`：短问答
  - `prompt-2`：200-400 字文本总结
- 指标：`response`、`耗时`、`显存变化`、`输出 token 大致范围`
- 对照组：`同模型不同 prompt 长度`
- 预期观察：`长 prompt 使 prefill 更重，TTFT 更敏感`

## 本周交付物

- [ ] 文档：`最小本地推理记录`（补齐）
- [ ] 文档：`V100 环境说明文档`（补齐）
- [ ] 笔记：`Karpathy_摘记_2026-W11`
- [ ] 笔记：`KV_Cache_摘记_2026-W11`
- [ ] 笔记：`Metrics_映射_2026-W11`
- [ ] 一张图：`推理请求最小生命周期图`

## 风险与阻塞

- 当前最大阻塞：基础概念理解偏细节导向，单日推进速度低于理想计划。
- 需要缩小范围的点：本周不引入新框架安装，不开 vLLM/SGLang 实操坑，只做阅读+最小记录闭环。
- 降难策略：允许把 `Karpathy -> training loop -> KV Cache -> metrics` 拉成 4 个顺序台阶，不要求两天内跨多个台阶。
- `micrograd` 使用原则：只把它当作 `backward / gradient` 的补盲材料，不把它升级为本周主线，不追求完整刷完。

## micrograd 补充策略（低优先级，不抢主线）

- 适用时机：只有当你对 `loss.backward()` 仍然停留在口号级理解时，才补 `micrograd`
- 本周投入上限：`45-60m`
- 只看这三个点：
  - `computation graph` 是什么
  - `chain rule` 为什么能把梯度逐层传回去
  - `backward` 最终产出的是参数梯度
- 本周不做的事：
  - 不追求看完整个 `micrograd`
  - 不手写完整自动求导系统
  - 不因为 `micrograd` 推迟 `KV cache` 和周日实验
- 完成标准：
  - 你能用自己的话说清：`loss.backward()` 不是“更新参数”，而是“先把梯度算出来`

## 本周执行原则

- [ ] 宁可少学一块，也要把一块讲清、写清、复述清。
- [ ] 每天只推进一个主问题，不并行打开多个新主题。
- [ ] 如果一个概念还不能用自己的话解释，就不急着进入下一个概念。

## 执行检查点（每日 5 分钟）

- [ ] 今天是否有“可见产出”（笔记/图/记录）？
- [ ] 今天是否只做了一个主线任务而非多线程跳转？
- [ ] 今天是否把新结论回写到周记而非散落聊天记录？

## 本周复盘（周日填写）

- 本周最重要的收获：
- 本周最真实的瓶颈：
- 本周最有价值的证据：
- 哪些事情不值得继续投入：
- 下周唯一主线：
