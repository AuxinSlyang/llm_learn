---
type: weekly_note
week: 2026-W13
annual_plan: [[LLM_Infra_Annual_Plan_2026]]
annual_plan_week: 2026-04 / Week 1
time_budget: 14-16h
track: LLM Inference / Serving
gpu_env: V100
---

# Week 2026-W13 学习记录

## 本周锚点

- 年度计划周次：`2026-04 / Week 1`
- 日期范围：`2026-03-23 ~ 2026-03-29`
- 本周主题：`先收口 makemore，再用 micrograd 补 backward 直觉，不并行开系统支线`
- 本周硬产出：`makemore 第一集收口` + `micrograd 前后半承接` + `1 份下周承接点`

## 本周和转岗目标的关系

这是 `W12` 的延续周，不是系统主题扩张周。  
这周的任务是先把 `makemore` 的最小训练闭环收口，再用 `micrograd` 把 `backward / gradient / 参数更新` 的直觉补起来，避免刚搭起地基就切去系统话题。

## 本周唯一主线

- [ ] 收口 `makemore` 第一集，并用 `micrograd` 补上 `backward` 的最小直觉

## 本周最低完成线

- [x] 看完 `makemore` 剩余主体内容
- [ ] 把 `训练闭环` 压成 `5-8` 句版本
- [ ] 把 `generate` 与 `training` 的分叉写清楚
- [ ] 留下 `1` 份关键概念最小笔记
- [ ] 看完 `micrograd` 前半部分
- [ ] 看完 `micrograd` 后半部分
- [ ] 给 `W14` 留下 `2-3` 个明确入口任务

## 本周目标产物

- [x] 笔记 1：`makemore 第一集收口笔记`
- [ ] 笔记 2：`训练闭环 5-8 句版本`
- [ ] 卡片 1：`embedding / logits / cross entropy / backward / generate`
- [ ] 笔记 3：`micrograd 前后半最小理解`

## 时间预算（14-16h）

- [x] 周一 2h：看完并收口 `makemore` 第一集，补 `training / generate / loss`
- [ ] 周二 2h：看 `micrograd` 前半部分，先把 `backward` 的最小直觉搭起来
- [ ] 周三 2h：看 `micrograd` 后半部分，继续补 `gradient / 参数更新 / 计算图`
- [ ] 周四 2h：继续 `makemore`，同步整理最小笔记
- [ ] 周五 1-1.5h：回看本周笔记，补 `1-2` 个没吃透的点
- [ ] 周六 2h：收口 `makemore` 主体观看和关键概念
- [ ] 周日 3-4h：写周复盘，并给 `W14` 留入口

## 按你的作息重排（日计划梗概）

### 周一
- 视频：看完并收口 `makemore` 第一集
- 整理：补 `Bigram / logits / probs / loss`
- 输出：写 `训练和生成从哪里分叉`

### 周二
- 视频：看 `micrograd` 前半部分
- 整理：先建立 `backward / gradient` 的最小直觉
- 输出：记 `3-5` 条关键理解，不追一口气吃透全部细节

### 周三
- 视频：看 `micrograd` 后半部分
- 整理：回看前半部分，接上 `参数更新 / 计算图`
- 输出：补一版 `backward` 最小解释

### 周四
- 阅读：回看卡住的 `cross entropy / backward / generate`
- 整理：把 `makemore + micrograd` 串成可复述版卡片
- 输出：继续压 `训练闭环` 最小解释

### 周五
- 轻量回看：补 `1-2` 个卡点，不新开主题

### 周六
- 收口 `makemore` 主体观看和阶段性笔记

### 周日
- 写一页复盘
- 给 `W14` 留 `prefill / decode / KV cache` 的明确入口

## 本周输入材料

- [x] `Karpathy makemore` 第一集
- [ ] `micrograd` 前半部分
- [ ] `micrograd` 后半部分
- [ ] 你在 `W12` 留下的 `makemore / training vs inference` 笔记
- [ ] `Attention Is All You Need`（本周默认非必须，只有看完 makemore 后再回看）

## 本周任务清单

### 理论与概念
- [ ] `generate` 为什么只取最后一个时间步
- [x] `training` 和 `inference` 从哪里开始分叉
- [ ] `backward` 在计算图上到底做了什么
- [ ] `attention` 在做什么（本周非必须）
- [ ] `Q / K / V` 分别是什么（本周非必须）

### 阅读与理解
- [ ] 回答 `embedding / logits / cross entropy / backward / optimizer.step / generate`
- [ ] 把 `训练闭环` 压成 `5-8` 句
- [ ] 把 `训练 vs 推理` 的分叉写清

### 笔记与输出
- [x] 写 `makemore 主体阶段性收口笔记`
- [ ] 写 `训练闭环 5-8 句版本`
- [ ] 写 `复述版概念卡片`
- [ ] 写 `micrograd` 最小理解笔记

## 风险与阻塞

- 当前最大阻塞：`backward` 还停在直觉层，容易在公式细节里卡太久。
- 需要缩小范围的点：本周不切 `KV cache / metrics / vLLM / SGLang`，也不把 `attention` 当成必须项。
- 降难策略：优先保 `makemore 收口 + micrograd 前后半 + 训练闭环`，不追一次讲全所有数学细节。

## 本周执行原则

- [ ] 继续只保 `makemore` 一条主线
- [ ] 在 `makemore` 收口后，只承接 `micrograd`，不再扩新主题
- [ ] 每个概念都要能用自己的话复述
- [ ] 先完成主体观看，再做概念收口，不在中途不断扩任务

## 本周复盘

- 本周最重要的收获：`把 makemore 第一集真正收口了，并把 micrograd 主体内容推进完一轮；现在对训练闭环、training vs generate 分叉、以及 backward 在计算图上反向传播这几件事，已经不再是完全抽象的词。`
- 本周最真实的瓶颈：`不是完全没推进，而是“看过”和“吃透”之间还有距离。micrograd 虽然已经看完主体并串到 nn 封装，但还差最后一次代码级 review 才算真正内化；makemore 也只完成了第一集阶段性收口，离整套内容吃透还有明显距离。`
- 本周最有价值的证据：`已经留下 makemore 第一集收口笔记、training vs generate 分叉说明，以及一组 micrograd 前半 / 后半 / nn 封装的阶段性日记与总结；这些材料足够支撑下周继续做“吃透”而不是重头开始。`
- 哪些事情不值得继续投入：`下周不适合按 4 月计划直接切去 prefill / decode / KV cache / TTFT / TPOT；如果模型地基还没压实，提前切系统词汇只会继续变成“听过但讲不清”。`
- 下周唯一主线：`先用约 1h 把 micrograd 做最后一次代码级收口，然后把剩余主时间压到 makemore，目标不是只看完，而是把 micrograd 和 makemore 都讲到“能用自己的话复述”。`
