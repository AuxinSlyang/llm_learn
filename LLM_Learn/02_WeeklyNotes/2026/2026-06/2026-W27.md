---
type: weekly_note
week: 2026-W27
annual_plan: [[03_Annual_Plan_2026]]
annual_plan_week: 2026-06 / Week 5 / July transition
time_budget: 6-10h
paper_slot: DeepSeek-V2 / Transformer implementation support
track: AI Core Storage / Transformer implementation refresh / TokaDB TabletServer transition
env: local Mac + optional dev1
---

# Week 2026-W27 学习记录

## 本周锚点

- 日期范围：`2026-06-29 ~ 2026-07-05`
- 本周主题：`DeepSeek Storage/In­ference 路线切换后的第一周：Transformer/GPT-2 结构补齐 + DeepSeek-V2 读完 + TokaDB TabletServer 入口准备`
- 本周硬产出：`DeepSeek-V2_MLA_MoE_Note` + `Karpathy_GPT2_Transformer_Block_Refresh` + `TabletServer_Request_Path_Index`
- 本周完成标准：能手写一个 decoder-only Transformer block 的核心结构，能讲清 DeepSeek-V2 的 MLA/DeepSeekMoE 为什么分别对应 KVCache 和 FFN 计算瓶颈，并为 7 月 TabletServer + mini-lsm 主线准备入口。

## 本周唯一主线

- [ ] 把路线切换落到可执行基本功：先用 Karpathy GPT-2 复现和手写 decoder-only Transformer block 补齐 QKV / MHA / FFN / decode path，再接 DeepSeek-V2 的 MLA / MoE / KVCache 结构理解。

## 本周最低完成线

- [ ] 看完 Karpathy GPT-2 视频的 Transformer block / decode 相关部分，写 `Karpathy_GPT2_Transformer_Block_Refresh`。
- [ ] 手写一个最小 decoder-only Transformer block：RMSNorm、QKV projection、multi-head attention、residual、FFN/MLP、logits head；不要求训练完整模型。
- [ ] 读完 DeepSeek-V2 的 Abstract / Introduction / MLA / DeepSeekMoE / efficiency sections，并回答至少 4 个 Reading Questions。
- [ ] 为 W27 代码主线建立 `TabletServer_Request_Path_Index`：先索引 RPC/Admin RPC、ReplicaManager、Replica/FSM、EngineFactory 的入口。
- [ ] 把 speculative decoding / DSpark 放入后续队列，不在本周抢主线。

## 本周 Top 3

- [ ] `Transformer 基本功`：Karpathy GPT-2 视频 + 手写 decoder block。
- [ ] `DeepSeek-V2 结构理解`：MLA 对应 KVCache，DeepSeekMoE 对应 FFN sparse compute。
- [ ] `TokaDB 入口准备`：只做 TabletServer request path 索引，不展开大规模源码深挖。

## 时间预算

- [ ] 周一：Karpathy GPT-2 视频：Transformer block / attention / MLP / logits；写第一版结构笔记。
- [ ] 周二：手写最小 decoder-only block，跑通 shape，不追训练效果。
- [ ] 周三：DeepSeek-V2 MLA / DeepSeekMoE 复盘，把 Transformer block 结构映射到论文结构。
- [ ] 周四：TokaDB TabletServer request path 入口索引。
- [ ] 周五：补 mini-lsm / RocksDB 本周入口，确定 7 月第一周代码任务。
- [ ] 周末：W27 复盘，确认 `Transformer refresh -> DeepSeek-V2 -> TabletServer/mini-lsm` 是否闭环。

## 本周任务清单

### Transformer / GPT-2 Refresh

- [ ] 看 Karpathy GPT-2 视频中和 tokenizer / embedding / Transformer block / attention / MLP / logits 直接相关的部分。
- [ ] 写 `Karpathy_GPT2_Transformer_Block_Refresh`，只记录 decoder-only block 的 shape flow 和实现边界。
- [ ] 手写一个最小 block，至少跑通 `[B, T, d_model] -> [B, T, vocab_size]` 的 forward shape。

### DeepSeek-V2 / MLA / MoE

- [ ] 完成 `DeepSeek-V2_MLA_MoE_Note`：Abstract / Introduction / MLA / DeepSeekMoE / efficiency。
- [ ] 回答：MLA 缓存什么？为什么 decoupled RoPE？DeepSeekMoE 为什么是 FFN 层优化？KVCache reduction 如何影响 serving throughput？
- [ ] 把 DSpark / speculative decoding 只作为后续 queue，不展开读。

### TokaDB / July Bridge

- [ ] 建立 `TabletServer_Request_Path_Index`，只列入口文件、主要类、请求流向和还没懂的问题。
- [ ] 确认 7 月第一周 `TabletServer + mini-lsm/RocksDB` 的最小切入点。
- [ ] 保留机器人为长期 North Star，本周不推进硬件组装和 VLA mapping。

## 本周交付物

- [ ] `Karpathy_GPT2_Transformer_Block_Refresh`
- [ ] `DeepSeek-V2_MLA_MoE_Note`
- [ ] `TabletServer_Request_Path_Index`
- [ ] `2026-07_to_2027-04_DeepSeek_Storage_Inference_详细周任务.md` 如有必要补一行 GPT-2 refresh。
- [ ] `2026-06 月末复盘`。

## 风险与阻塞

- 当前最大风险：同时想补 Transformer、DeepSeek-V2、TokaDB、speculative decoding，导致本周没有一个可验收输出。
- 需要缩小范围的点：本周不读完整 DeepSeek 系列，不读完整 speculative decoding 包，不做机器人硬件/VLA。
- 降级策略：如果时间不够，优先完成 `Transformer block shape flow + DeepSeek-V2 4 个 Reading Questions`；TabletServer 只列入口索引。

## 本周复盘

- 本周最重要的收获：
- 本周最真实的瓶颈：
- 本周最有价值的证据：
- 哪些事情不值得继续投入：
- 下周唯一主线：`TokaDB TabletServer request path + mini-lsm/RocksDB core mechanism`
