---
type: weekly_note
week: 2026-W27
annual_plan: [[03_Annual_Plan_2026]]
annual_plan_week: 2026-06 / Week 5 / July transition
time_budget: 6-10h
paper_slot: LSM in a Week / RocksDB-LSM support
track: LSM / RocksDB / TokaDB TabletServer data path
env: local Mac + optional dev1
---

# Week 2026-W27 学习记录

## 本周锚点

- 日期范围：`2026-06-29 ~ 2026-07-05`
- 本周主题：`7 月 LSM/TokaDB 主线启动：LSM in a Week 入口 + TabletServer request path index + RocksDB/LSM 对照`
- 本周硬产出：`TabletServer_Request_Path_Index` + `TinyLSM_W1_Memtable_Iterator_Log` + `RocksDB_LSM_Refresh_v0`
- 本周完成标准：能讲清 LSM write/read path 的最小机制，能定位 TokaDB TabletServer 请求进入 Replica/FSM/Engine 的入口，并把 mini-lsm 的 memtable / iterator 和 RocksDB/TokaDB 的生产实现边界对照起来。

## 2026-06-29 用户校准

本月目标明确收敛为：

```text
LSM 相关代码机制
+ TokaDB TabletServer 数据链路核心代码
+ RocksDB / LSM 代码学习
```

因此本周不再把 GPT-2 refresh / DeepSeek-V2 作为后续主线。DeepSeek-V2 已作为 KVCache 背景完成分析；本周剩余时间直接进入 7 月 storage code 主线。

补充校准：DeepSeek-V2 可以继续看，但定位改为课外读物；晚上主线固定给 mini-lsm / LSM 代码和 Rust 支撑能力。

## 本周唯一主线

- [ ] 启动 7 月 storage code 主线：用 `LSM in a Week` / mini-lsm 建立 LSM 机制感，同时索引 TokaDB TabletServer request path，不扩散到推理系统或机器人硬件。

## 本周最低完成线

- [ ] 阅读 `LSM in a Week.pdf` 的 Preface / Course Overview / Write Path / Read Path，建立本月代码学习地图。
- [ ] mini-lsm Week 1 入口：完成或至少读通 `1.1 Memtable`、`1.2 Merge Iterator` 的代码任务和测试入口。
- [ ] Rust 支撑：只补 mini-lsm 代码里遇到的 ownership、iterator trait、`Arc` / `Mutex`、error handling、tests，不开泛 Rust 课程。
- [ ] 建立 `TabletServer_Request_Path_Index`：索引 Tablet RPC / Admin RPC、ReplicaManager、Replica、ReplicaFsm、EngineFactory 的入口文件、关键类和问题清单。
- [ ] 写 `RocksDB_LSM_Refresh_v0`：先只覆盖 WAL、memtable、SST、iterator、Bloom/filter、compaction 的机制地图。
- [ ] DeepSeek-V2 / GPT-2 / speculative decoding 放入后续背景，不在本周剩余时间抢主线。

## 本周 Top 3

- [ ] `LSM in a Week 入口`：读课程 overview，明确 Week 1/2/3 和本月取舍。
- [ ] `TinyLSM W1`：memtable + merge iterator，建立测试/CLI 基线。
- [ ] `TokaDB TabletServer index`：只做入口索引，不展开大规模源码深挖。

## 时间预算

- [x] 周一：DeepSeek-V2 完成分析，作为 KVCache 背景；晚间校准 7 月主线为 LSM / TabletServer / RocksDB。
- [ ] 周二：读 `LSM in a Week` overview + mini-lsm 环境/Week1 入口，写 `TinyLSM_W1_Memtable_Iterator_Log` 骨架。
- [ ] 周三：mini-lsm `Memtable / Merge Iterator` 代码与测试；同步写 LSM write/read path 机制图。
- [ ] 周四：TokaDB TabletServer request path 入口索引：RPC/Admin RPC -> ReplicaManager -> Replica/FSM -> Engine。
- [ ] 周五：RocksDB/LSM refresh：WAL / memtable / SST / iterator / Bloom/filter / compaction 机制地图。
- [ ] 周末：W27 复盘，确认 `LSM mechanism -> TabletServer path -> RocksDB comparison` 是否形成第一版闭环。

## 本周任务清单

### LSM in a Week / mini-lsm

- [ ] 读 `/Users/bytedance/Desktop/LSM in a Week.pdf` 的 Preface / Course Overview / Write Path / Read Path。
- [ ] 建立 mini-lsm 学习日志：`TinyLSM_W1_Memtable_Iterator_Log`。
- [ ] 完成或读通 Week 1 Day 1/2：memtable、merge iterator、测试入口。
- [ ] 写出 LSM read/write path 的第一版图：WAL、memtable、immutable memtable、SST、compaction、manifest。

### Rust for mini-lsm

- [ ] 记录 `Rust_for_mini_lsm_Gap_List`：只写代码中真实遇到的 Rust 缺口。
- [ ] 优先级：ownership / borrow checker、iterator trait / trait object、`Arc` / `Mutex`、background thread、`Result` / error propagation、test pattern。

### DeepSeek-V2 Extracurricular

- [ ] 每次 20-40m 继续读 DeepSeek-V2 或回看 note，定位为课外读物。
- [ ] 只补 `KVCache / inference efficiency / MoE systems intuition`，不占用晚间 mini-lsm coding block。

### RocksDB / LSM Refresh

- [ ] 写 `RocksDB_LSM_Refresh_v0`，先不追源码深水，只建立机制地图。
- [ ] 对照 `TokaDB_RocksDB_OpenClose_Map`，明确 Open/Close / MANIFEST / WAL / CF options 与 LSM recovery 的关系。

### TokaDB / July Bridge

- [ ] 建立 `TabletServer_Request_Path_Index`，只列入口文件、主要类、请求流向和还没懂的问题。
- [ ] 确认 7 月第一周 `TabletServer + mini-lsm/RocksDB` 的最小切入点。
- [ ] 保留机器人为长期 North Star，本周不推进硬件组装和 VLA mapping。

## 本周交付物

- [ ] `TabletServer_Request_Path_Index`
- [ ] `TinyLSM_W1_Memtable_Iterator_Log`
- [ ] `Rust_for_mini_lsm_Gap_List`
- [ ] `RocksDB_LSM_Refresh_v0`
- [ ] `2026-07_to_2027-04_DeepSeek_Storage_Inference_详细周任务.md` 如有必要同步本周 LSM/TokaDB 校准。
- [x] `2026-06 月末复盘`。

## 风险与阻塞

- 当前最大风险：同时想学 LSM、RocksDB、TokaDB、KVCache、3FS，导致本月没有代码证据。
- 需要缩小范围的点：本周不读完整 RocksDB 源码，不深挖 3FS / KVCache / inference runtime，不做机器人硬件/VLA。
- 降级策略：如果时间不够，优先完成 `LSM overview + TabletServer_Request_Path_Index`；mini-lsm 代码只做 memtable / iterator。

## 本周复盘

### 本周完成

- 完成 7 月主线校准：本月 P0 从泛 AI Infra / robot learning 收敛到 `LSM / RocksDB / TokaDB TabletServer data path`。
- 建立 MiniLSM Week 1 入口：dev2 repo / test gate / W1D1 MemTable 问题清单 / sandbox patch 验证记录已经形成，`TinyLSM_W1_Memtable_Iterator_Log` 可作为后续代码证据主线。
- 开始补 Rust for MiniLSM：ownership / borrow / slice 已和 `&self`、`&[u8]`、`Bytes`、`SkipMap`、`Result<Option<Bytes>>` 建立连接。
- 启动 DDIA 系统语言支撑：Ch1 / Ch2 first pass 已写入 notes，能把 data system 的 reliability / scalability / maintainability 与 storage engine / TabletServer / KVCache storage 对齐。

### 本周未完成

- MiniLSM Week 1 没有完成真实 dev2 repo 的 Day 1 gate，Block / SST / read path / write path 也未开始。
- `TabletServer_Request_Path_Index` 和 `RocksDB_LSM_Refresh_v0` 仍未形成可复用交付。
- Rust Ch5 / Ch6 / collections / error handling / iterator / smart pointers 只完成入口定位，尚未形成足够支撑周日 coding 的完整 checklist。

### 本周阻塞 / 风险

- 最大瓶颈是 Rust 基础不足导致 MiniLSM coding 容易卡在 ownership、shared state、iterator、`Result<Option<_>>` 等语言/系统边界。
- 第二个风险是学习主题容易扩散到 DDIA、RocksDB、TokaDB、KVCache、3FS，导致本月缺少可运行代码证据。
- dev2 与本地验证之间存在割裂：本地 sandbox patch 已验证过 Day 1，但真实 repo 仍需要重新跑 gate，不能把 sandbox 结果当作正式完成。

### 下周承接

- 下周唯一主线：`mini-lsm Block/SST/Read-Write Path + TabletServer read/write path + RocksDB 对照`。
- 最低完成线：先在真实 repo 完成 MiniLSM Day 1 MemTable gate，再推进 MergeIterator / Block / SST；同时补一版 `TabletServer_Request_Path_Index`，只列入口文件、主类和未懂问题。
- DDIA Ch3 `Storage and Retrieval` 只作为 LSM / RocksDB 对照材料，不单独扩成泛书籍阅读线。

### 是否需要调整月计划

- 暂不调整 7 月计划；本周 slip 的处理方式是降低 W27 对 MiniLSM Week 1 完成范围的预期，把 W28 前半周用于补齐 Day 1-3 代码证据。
