---
type: weekly_note
week: 2026-W25
annual_plan: [[03_Annual_Plan_2026]]
annual_plan_week: 2026-06 / Week 3
time_budget: 20-27h (Mon-Thu 2-3h/day, Fri-Sun 4-5h/day)
paper_slot: project_explainer + weekend paper sprint
track: Robot Learning Full-Stack / SO-ARM101 coding first loop / CV closure / VLA-LLM-World Model awareness
env: local + SO-ARM101
---

# Week 2026-W25 学习记录

## 本周锚点

- 日期范围：`2026-06-15 ~ 2026-06-21`
- 本周主题：`SO-ARM101 项目 coding first loop：hardware validation -> teleop -> record -> replay -> policy/eval scaffold`
- 本周硬产出：`E001_hardware_bringup` + `E002_dataset_recording` + `E003_replay` + `project coding scaffold` + `CV closure map` + `VLA/LLM/World Model bridge notes`
- 本周完成标准：机械臂组装和 LeRobot 主路径推进到七七八八；CV backbone 线收口；VLA / LLM / World Model 达到能讲清接口、训练范式和和 LeRobot 的关系的程度。

## 2026-06-15 范围校准：本周要“搞定”的 LeRobot 是哪一层

结论：本周可以把 `LeRobot 首闭环主路径` 搞定，难度可控；但不把“整个 LeRobot 生态”作为验收目标。

### P0：本周必须搞定

- `SO-ARM101 + LeRobot` 的执行主路径：

```text
tool gate / hardware validation
-> lerobot-find-port
-> lerobot-setup-motors
-> lerobot-calibrate
-> lerobot-teleoperate
-> lerobot-record
-> lerobot-replay
```

- 对应证据：
  - `E001 Hardware Bring-Up` 有真实工具、端口、电机、calibration 或 blocker 记录。
  - `E002 Dataset Recording` 至少进入命令和数据路径设计；硬件顺利时录制 3-5 条短 episode。
  - `E003 Replay` 至少 replay 1 条真实或 mock episode。
  - `project_coding_scaffold.md` 固化命令、dataset path、episode log、failure log、eval stub。

### P1：顺利时推进

- `data_schema.md` 明确 `observation.images / observation.state / action / task / episode metadata`。
- 固定第一个任务：优先 `push-to-zone`，成功标准简单、可重复、便于失败分析。
- 看懂 `LeRobotDataset` 的最小落盘结构：`parquet + mp4 + meta`，不做完整源码精读。

### P2：本周不追

- 不训练 ACT/BC v0 作为硬验收；最多准备命令和数据前置。
- 不进入 `SmolVLA / pi0 / OpenVLA / async inference` 源码；论文可以做 structured / awareness read，但不把源码复现作为本周目标。
- 不追成功率、不改机械结构、不买 Jetson / RealSense / 3D 打印机。
- 不把 LeRobot 全部 policy / simulator / RL / annotation / reward model 都读完。

## 2026-06-15 加码计划：LeRobot 完成后读 ViT / pi0 / 关键论文

判断：周五、周六、周日有休息日学习块，可以加论文量；但顺序必须是：

```text
先拿 LeRobot 首闭环证据
-> 再读 ViT / pi0
-> 最后做论文连接和 W26 计划
```

### 本周论文硬目标

- [ ] `ViT`：完成 structured read，回答 `image -> patch tokens -> Transformer encoder -> visual representation`，并连接到 CLIP / VLM / VLA visual encoder。
- [ ] `pi0`：完成 structured awareness read，回答 VLM backbone、action expert、flow matching action generation、runtime/control frequency 四个问题。

### 本周论文软目标

按 LeRobot 进度选择，不要求全部完成：

- [ ] `ACT`：如果本周已经 record/replay，优先读 ACT，服务下周 ACT/BC v0。
- [ ] `Diffusion Policy`：如果 pi0 的 flow/action generation 看不稳，用它补连续动作生成直觉。
- [ ] `Vision Transformers Need Registers`：如果 ViT 读完后还有精力，作为 visual token / attention artifact 支撑线。
- [ ] `OpenVLA / SmolVLA`：只做 30m awareness，对齐后续 VLA deployment；不进源码、不训练。

### 论文不要抢主线的规则

- 每天先问：`E001/E002/E003 今天有没有新增证据？`
- 如果没有新增证据，论文只能做 20-40m 支撑槽位。
- 如果 LeRobot 卡住，必须先写 blocker / mock path，再读论文。
- 本周不追“读很多”的数量感；优先保证每篇留下 3 个项目连接：
  - 它的 observation 是什么？
  - action / output 是什么？
  - 对 LeRobot / SO-ARM101 / policy runtime 有什么用？

## 2026-06-15 二次校准：本周三条线

用户时间预算：

- 周一到周四：每天 `2-3h`。
- 周五到周日：每天 `4-5h`。
- 总预算约 `20-27h`。

本周不再只是“做一点 LeRobot + 随便读论文”，而是三条线并行收口：

```text
P0 工程硬主线：LeRobot / SO-ARM101 首闭环主路径
P1 知识收口线：CV backbone -> visual tokens
P2 概念桥接线：VLA + LLM + World Model 到够用程度
```

### P0：LeRobot / SO-ARM101 七七八八

本周目标不是完整训练 policy，而是把真实机器人项目推进到“下周可以录数据 / 训练 ACT v0”的状态。

最低完成：

- [ ] 机械臂装配、固定、线材、电源、USB、camera 状态清楚。
- [ ] `E001 Hardware Bring-Up` 有真实记录：工具状态、清点、端口、电机 ID、calibration 或明确 blocker。
- [ ] LeRobot 命令路径跑通或 walkthrough 到位：

```text
find-port -> setup-motors -> calibrate -> teleoperate -> record -> replay
```

- [ ] `project_coding_scaffold.md`、`data_schema.md`、`failure_log / eval stub` 能作为下周入口。

绿色完成：

- 能 teleop。
- 能 record 至少 1-3 条 episode。
- 能 replay 至少 1 条 episode。

黄色完成：

- 硬件某步卡住，但 blocker、mock path、命令路径和数据 schema 都明确。

### P1：CV 收口

CV 本周的目标不是继续扩全科，而是把 `robot observation -> visual representation -> visual token` 这条线收住。

必须完成：

- [ ] `ViT` structured read：patch embedding、class token、position embedding、data scale、和 ResNet/CLIP 的关系。
- [ ] 写出 8-12 条 CV closure notes，串起：

```text
LeNet -> AlexNet -> VGG -> GoogLeNet/Inception -> ResNet -> ViT -> CLIP/VLA visual encoder
```

- [ ] 明确每类视觉 backbone 对 LeRobot 的意义：
  - CNN：局部特征、层级表征、backbone。
  - Inception：compute-aware / multi-scale。
  - ResNet：深层优化和 residual path。
  - ViT：image patches as tokens，连接 VLM/VLA。

可选完成：

- [ ] `Vision Transformers Need Registers` awareness：只回答 ViT attention artifact / register tokens 是什么。
- [ ] `CLIP` 30-45m bridge：图文对齐如何成为 VLA visual-language grounding 的入口。

### P2：VLA / LLM / World Model 到“够用程度”

这里的“够用”不是精读和复现，而是能建立三张概念图。

#### VLA 够用

- [ ] `pi0` structured awareness：VLM backbone、action expert、flow matching、action sequence、runtime/control frequency。
- [ ] 建立 action representation 对照：

```text
ACT: action chunk
RT-2: action as token
Diffusion Policy: action denoising
pi0: flow matching action expert
LeRobot: observation/state/action dataset interface
```

#### LLM 够用

- [ ] 补一个 LLM 支撑线收口块，只回答：

```text
pretraining -> post-training -> RL
```

如何类比到：

```text
VLA / WAM:
world/action pretraining -> robot/task post-training -> RL / environment feedback
```

- [ ] 不重开 LLM 论文队列；只把 nanoGPT / tokenizer / post-training / RLHF 连接到 VLA/WAM。

#### World Model 够用

- [ ] `World Models` 或 `DreamerV3` 选一篇做 awareness scan。
- [ ] 输出 5-8 条：

```text
world model 学什么
和 policy / VLA 的关系是什么
为什么它可以服务 planning / imagination / RL
和 WAM / Jim Fan 三阶段类比怎么接
```

不做：

- 不开完整 model-based RL 专题。
- 不复现 Dreamer。
- 不把 World Model 抢成新主线。

## 本周唯一主线

- [ ] 工程唯一主线：让第一个实物机器人项目真的动起来，并开始项目 coding；不追训练效果，先保住 `硬件 -> teleop -> record -> replay -> project/eval scaffold`。
- [ ] 学习收口线：用休息日时间把 CV、VLA、LLM、World Model 读到能服务 LeRobot / policy runtime 的程度。

## 本周最低完成线

- [ ] 完成或推进 SO-ARM101 组装与连接：电源、USB、端口、桌面固定。
- [ ] 完成电机 ID / calibration 记录，能说明每个失败点。
- [ ] 完成一次 teleoperation。
- [ ] 录制 3-5 条短 episode。
- [ ] replay 至少 1 条 episode。
- [ ] 建立第一版项目代码结构：命令记录、dataset path、episode log、failure log、eval stub。
- [ ] 如果硬件未到：完成 LeRobot 安装/命令 walkthrough、mock dataset 或 Gymnasium smoke test，并写 blocker report。

## 本周 Top 3

- [ ] `LeRobot 七七八八`：组装、端口、电机、校准、teleop/record/replay 主路径尽量跑通；跑不通就写清 blocker。
- [ ] `CV 收口`：完成 ViT，并写出 `CNN backbone -> visual tokens -> VLA visual encoder` 的闭环说明。
- [ ] `VLA / LLM / World Model 够用层`：完成 pi0 awareness、LLM 三阶段类比、World Model awareness，并全部回接到 SO-ARM101 / LeRobot。

## 时间预算

- [ ] 周一（2-3h）：定本周目标 + tool gate + E001 第一条真实观察或 blocker；创建/补齐 `project_coding_scaffold.md`。
- [ ] 周二（2-3h）：SO-ARM101 assembly / parts cleanup / ports / motor ID；若阻塞，完成 `find-port -> setup-motors -> calibrate` walkthrough。
- [ ] 周三（2-3h）：calibration + teleoperation；如果不能真机 teleop，至少完成 teleop 命令草稿、前置条件和 blocker。
- [ ] 周四（2-3h）：record / replay 前置；固定 `push-to-zone` 任务、camera 视角、dataset path、episode log。
- [ ] 周五（4-5h）：LeRobot 补缺 + replay / blocker；然后完成 `ViT` structured read 和 CV closure notes。
- [ ] 周六（4-5h）：上午优先补 LeRobot；下午读 `pi0` + action representation 对照；晚上补 LLM 三阶段到 VLA/WAM 的类比。
- [ ] 周日（4-5h）：World Model awareness + 本周连接总结 + 下周 ACT/BC v0 入口；有余力再补 ACT / Diffusion Policy / Registers 三选一。

## 本周任务清单

### Hardware Bring-Up

- [ ] 机械臂组装与固定。
- [ ] 端口识别和命令记录。
- [ ] 电机 ID / calibration 文件记录。
- [ ] follower 跟随 leader。

### Dataset / Replay

- [ ] 固定一个最简单任务：优先 `push-to-zone`。
- [ ] 录制 3-5 条短 episode。
- [ ] replay 1 条，观察动作质量。
- [ ] 记录 camera 视角、桌面布局、物体初始状态。

### Project Coding

- [ ] 建立 `scripts/` 或命令记录入口：find-port、setup-motors、calibrate、teleop、record、replay、eval。
- [ ] 建立 `episodes/` 或 dataset path 约定。
- [ ] 建立 `failure_log` / `run_log` 模板。
- [ ] 建立最小 eval stub：读取 episode metadata，记录 success/failure/manual notes。
- [ ] 明确下一个 policy 目标：先 ACT/BC v0，不追 VLA 训练。

### LLM 支撑线

- [ ] 只保留 1 个 30-45m 收口块：补 `LLM phase 1 总结` 或 `tokenizer` 未完成项。
- [ ] 不新开 LLM 论文。

### Paper Sprint

- [ ] ViT structured read：patch embedding、class token、position embedding、data scale、和 ResNet/CLIP 的关系。
- [ ] pi0 structured awareness：VLM backbone、action expert、flow matching、multi-robot/multi-task、runtime 边界。
- [ ] World Model awareness：`World Models` / `DreamerV3` 二选一，不复现，只建立概念图。
- [ ] LLM support closure：`pretraining -> post-training -> RL` 如何迁移到 VLA/WAM。
- [ ] 论文连接总结：写 8-12 条 `CV / VLA / LLM / World Model -> LeRobot / SO-ARM101` 映射。
- [ ] 可选补一篇：ACT / Diffusion Policy / Vision Transformers Need Registers 三选一。

## 本周交付物

- [ ] `/Users/bytedance/.openclaw/workspaces/llm-learner/LLM_Learn/03_Projects/so-arm101-lerobot-first-loop/05_Experiment_Log.md` 更新 `E001 / E002 / E003`。
- [ ] `/Users/bytedance/.openclaw/workspaces/llm-learner/LLM_Learn/03_Projects/so-arm101-lerobot-first-loop/notes/data_schema.md` 初版。
- [ ] `/Users/bytedance/.openclaw/workspaces/llm-learner/LLM_Learn/03_Projects/so-arm101-lerobot-first-loop/notes/project_coding_scaffold.md` 初版。
- [ ] 如果阻塞：`hardware_blocker_report.md`。

## 本周验收口径

### 绿色完成

- SO-ARM101 能完成 teleop。
- 至少 1 条 episode 被 record 并 replay。
- `E001 / E002 / E003` 有真实命令、观察、失败和下一步。
- `project_coding_scaffold.md` 与 `data_schema.md` 可作为下周 ACT/BC v0 的入口。
- `ViT` 和 CV closure 完成。
- `pi0`、LLM 三阶段类比、World Model awareness 各自留下 structured takeaway，并明确连接到 LeRobot / SO-ARM101。

### 黄色完成

- 硬件某一步阻塞，但 blocker 写清楚。
- LeRobot 命令 walkthrough、mock dataset / replay path 和项目 scaffold 完成。
- 下周第一步明确到具体命令或具体硬件动作。
- `ViT` 完成；`pi0` 至少完成 awareness notes；World Model 或 LLM 类比至少完成一个。

### 红色未完成

- 只读 LeRobot / 论文，没有 E001/E002/E003 证据。
- 没有命令路径、没有 dataset path、没有 blocker report。
- 继续扩 OpenVLA/pi0/SmolVLA 或 CV/LLM 论文，导致首闭环没有推进。

## 风险与阻塞

- 当前最大阻塞：SO-ARM101 主体已到货，但摄像头 / 螺丝刀 / 桌面固定 / 供电线核对仍可能卡住；端口、电机 ID、calibration 和 teleop 都必须以真实命令输出为准。
- 需要缩小范围的点：本周不训练 ACT，不追成功率，不改机械结构。
- 降级策略：先完成 hardware validation 和 teleop；若工具继续阻塞，降级到 LeRobot 命令 walkthrough / mock dataset / Gymnasium smoke test，并写 `hardware_blocker_report.md`。

## 本周复盘

- 本周实际完成：
  - 完成 `ResNet -> ViT -> CLIP / BLIP-2 / LLaVA` 的 CV/VLM bridge 收口，能把 `image -> visual backbone -> visual tokens -> language-aligned visual embeddings -> assistant response` 讲清。
  - 完成 VLA 第一阶段阅读队列的分层和边界校准，把 `OpenVLA / pi0 / pi0-FAST` 收束为 action representation 三件套，不再继续扩 SmolVLA / pi0.5 / World Model。
  - 建立了 `OpenVLA / pi0 / pi0-FAST` paper note scaffold 和输出问题，明确要落到 LeRobot 的 `observation.images / observation.state / task / action / control frequency / latency`。
- 本周未完成与原因：
  - `SO-ARM101 / LeRobot` 没有推进到新的 E001 真实硬件证据；`05_Experiment_Log.md` 的最后事实仍停在 `2026-06-10` 开箱、工具和配件阻塞。
  - 没有进入 `teleop -> record -> replay`，也没有 E002/E003 数据或 replay 证据；原因是本周执行被 CV/VLM/VLA 阅读收束占用，硬件 gate 没有被优先处理。
  - `OpenVLA / pi0 / pi0-FAST` 还没有主文级 takeaway；目前主要是阅读入口、问题清单和 action matrix 草稿。
- 本周总结：
  - W25 在知识结构上有收获，但工程主线没有达标；下周必须先补真实硬件状态或 blocker，否则不能把 W26 直接当作数据/ACT 训练周。
- 本周最真实的瓶颈：
  - 计划里把 `record/replay/ACT` 建在 E001 已经推进的假设上，但实际 E001 没有新证据，导致后续数据闭环全部悬空。
- 本周最有价值的证据：
  - `2026-06-15` / `2026-06-16` Daily 的 CV/VLM bridge close-out。
  - `2026-06-17` Daily 与 `VLA_First_Stage_Reading_Plan.md` 的 VLA action representation 分层。
- 哪些事情不值得继续投入：
  - 在 E001 没有更新前，不继续扩大 VLA 论文雷达，不进入 OpenVLA/openpi/FAST 源码，不把 World Model 或 pi0.5 打开成新主题。

## 下周入口

- 下周唯一主线：先恢复 `SO-ARM101 / LeRobot` 真实证据，再进入数据和 ACT/BC v0。
- 下周最低完成线：
  - 周一先补 `E001 true status / blocker`：工具、供电、线材、桌面固定、USB、Mac 端口、3D 打印件支撑、舵机分拣。
  - 可安全推进时，只做下一条最小硬件动作或 `find-port` 前置观察；不可推进时，写 blocker + command walkthrough / mock replay path。
  - 只有 E001/E002 出现事实证据后，才把 W26 升级到 `record/replay -> ACT/BC v0 -> eval/failure taxonomy`。
- 下周建议顺序：
  1. `E001 true status / blocker`
  2. `project_coding_scaffold.md` command path / mock replay path
  3. `push-to-zone` task + dataset schema / episode log
  4. 有真实数据后再进入 ACT/BC v0
- 下周 Top 3：
  - `硬件事实恢复`：先写清工具/供电/线材/端口/固定状态。
  - `命令路径固化`：把 `find-port -> setup-motors -> calibrate -> teleoperate -> record -> replay` 的下一步写成可执行入口。
  - `数据闭环前置`：固定任务、camera plan、dataset path 和 failure log。
