---
type: monthly_plan
month: 2026-07
target_role: AI Core Infra / High-Performance Distributed Storage -> LLM Inference -> Robot/VLA Runtime
linked_roadmap: [[10_Systems_Thinking_for_AI_Infra_and_Robot_Runtime]]
supporting_roadmaps:
  - [[08_One_Year_Roadmap_LLM_Inference_to_Robot_Runtime]]
  - [[09_One_Year_Robot_Learning_Full_Stack_Roadmap]]
---

# 2026-07 月计划

## 本月定位

- 阶段：`M2 revised / DeepSeek Storage Route Foundation`
- 月主题：`LSM / RocksDB 代码机制 + TokaDB TabletServer 数据链路核心代码`
- 对当前主线的意义：先把当前 DB / 存储基本盘打成可复述、可对照、可面试的代码证据；KVCache / 3FS / 推理系统暂时作为后续连接，不抢本月 P0。

## 2026-06-29 用户校准：本月目标收敛

本月目标明确为三个 P0：

```text
1. 学会 LSM 相关代码机制：memtable / iterator / block / SST / read-write path / compaction / manifest / WAL / recovery
2. 读通 TokaDB TabletServer 数据链路核心代码：Tablet RPC / Admin RPC -> ReplicaManager -> Replica / ReplicaFsm -> Engine
3. 做 RocksDB 与 LSM 对照学习：RocksDB open/recovery、WAL、memtable、SST、iterator、Bloom/filter、compaction、write stall
```

本地材料入口：

- `LSM in a Week` PDF：`/Users/bytedance/Desktop/LSM in a Week.pdf`
- TinyLSM 项目计划：`LLM_Learn/08_Insights/Systems/storage/TinyLSM_First_Month_Project.md`
- TokaDB / RocksDB Open-Close map：`LLM_Learn/08_Insights/Systems/storage/TokaDB_RocksDB_OpenClose_Map.md`

执行约束：

- 本月 P0 不再是泛 `AI Core Storage / KVCache / 3FS / io_uring` 全展开。
- KVCache / 3FS / PagedAttention 只作为为什么要学好 storage engine 的后续背景，7 月不做主要验收。
- 每周至少一个代码链路图和一个机制对照：`mini-lsm 机制 -> RocksDB 工程实现 -> TokaDB 数据链路使用点`。
- 不追求把 mini-lsm 从零写完所有扩展；优先用它建立 LSM 机制感，再回到 TokaDB/RocksDB 的生产代码边界。
- `DeepSeek-V2` 可以继续作为课外读物推进，每次 20-40 分钟，只服务 `KVCache / inference efficiency / MoE systems intuition`，不抢晚间 mini-lsm 代码主线。
- 晚间默认主线：写 mini-lsm / LSM 代码，同时补 Rust 中和 storage engine 直接相关的能力：ownership、iterator trait、`Arc` / `Mutex`、background task、error handling、tests。

## 2026-06-28 修订：DeepSeek Storage 三级跳

短期职业判断调整为：

```text
分布式存储工程师
-> AI 推理工程师 / KVCache / serving / kernel 优化
-> 机器人系统工程师 / VLA runtime / robot data loop
```

理由：

- `高性能分布式存储工程师` 比纯机器人岗位更贴近当前履历：DB / 存储内核、分布式系统、性能优化和可靠性都能直接迁移。
- DeepSeek / AI Core Storage 的岗位问题足够核心：KVCache 存储、毫秒级延迟、上亿级 IOPS、分布式文件系统、对象存储、训练数据链路。
- 机器人方向仍然长期有吸引力，但短期还没到最适合切入的 turning point；更适合作为长期学习、自我探索和 AI Infra 应用牵引。
- 先进入 AI core infra，再从 KVCache / 推理服务 / 多模态 runtime 回接 VLA / robot policy runtime，比现在直接硬切机器人更稳。

执行约束：

- 7 月 P0 改为 `LSM / RocksDB / TokaDB TabletServer 数据链路`，不再把 Modern Robotics 或 KVCache Storage 作为主课。
- 机器人学习保留为 P2，每周最多一个轻量探索槽；不再用它挤占 storage / inference 主线。
- 所有系统学习都要先回到 `LSM storage engine + TabletServer data path` 的能力证据：WAL、memtable、SST、iterator、compaction、manifest、recovery、Replica/FSM/Engine 边界。
- 推理系统作为第二跳提前埋线，但 7 月只保留 KVCache / PagedAttention / 3FS 的背景意识，不安排成主要阅读或代码任务。
- 论文/材料阅读收敛到 `LSM in a Week / RocksDB docs / TokaDB code map`；泛 LLM、VLA、CV、Diffusion、Robot Learning 暂停主动新增。

## 2026-06-28 修订：Unitree GitHub 对齐

本节保留为机器人长期探索副线，不作为 7 月 P0。若本月有机器人学习时间，仍优先把课程学习绑定到 Unitree 官方软件栈：

```text
frame / pose / state / action
-> Unitree SDK2 / DDS / ROS2 message boundary
-> Unitree MuJoCo / IsaacLab sim-real interface
-> Unitree LeRobot data conversion / replay / train / eval
```

执行约束：

- `Modern Robotics` 从主课降级为 P2 支撑，不追连续章节进度；只在需要解释 robot state/action/runtime 时补。
- 7 月可以做 `unitree_sdk2_python`、`unitree_ros2`、`unitree_mujoco`、`unitree_lerobot` 的 lightweight walkthrough，但不能抢 storage / inference 主线。
- `unitree_sim_isaaclab`、`unitree_rl_lab`、`unifolm-vla`、`unifolm-world-model-action` 先作为 architecture / schema / deployment radar，不抢 7 月主线。
- SO-ARM101 + LeRobot 被重新定位为 `Unitree-style data/runtime first loop`：低成本练习 teleop、dataset、replay、eval、failure taxonomy 和 policy runtime 边界。
- 若 SO-ARM101 继续阻塞，7 月必须交付 blocker/report，并用 Unitree MuJoCo / SDK2 walkthrough 保持系统证据不断档。

## 本月最低完成线

- [ ] 完成 `LSM in a Week / mini-lsm Week 1`：memtable、merge iterator、block、SST、read path、write path、prefix key encoding、Bloom/filter。
- [ ] 完成 `LSM in a Week / mini-lsm Week 2 first pass`：compaction implementation、simple/tiered/leveled compaction、manifest、WAL、batch write、checksums。
- [ ] 建立 `Rust for mini-lsm` 最小学习清单：只围绕 LSM 代码中的 ownership / iterator / `Arc` / `Mutex` / error handling / tests，不展开泛 Rust 全科。
- [ ] 写出 `TabletServer_Request_Path_Index`：Tablet RPC / Admin RPC、ReplicaManager、Replica、ReplicaFsm、EngineFactory 的入口文件、关键类和问题清单。
- [ ] 写出 `TabletServer_Read_Write_Path_Map`：一条 read / write 从 RPC 入口到 Replica/FSM/Engine 的数据链路。
- [ ] 完成 `RocksDB / LSM storage engine refresh`：WAL、memtable、SST、compaction、snapshot、iterator、write/read/space amplification。
- [ ] 写出 `RocksDB_Read_Write_Path_Comparison`：把 mini-lsm 的机制和 RocksDB/TokaDB 的生产实现差异对齐。
- [ ] 写出 `RocksDB_Open_Recovery_WAL_Note`：Open/Close、MANIFEST、WAL replay、CF options、recovery contract。
- [ ] 机器人副线只保留一个最小承接：`SO-ARM101 blocker/report` 或 `Unitree repo map lightweight`，不作为本月验收核心。

## 本月关键产出

- [ ] `TinyLSM_W1_Memtable_Iterator_Log`
- [ ] `Rust_for_mini_lsm_Gap_List`
- [ ] `TinyLSM_Block_SST_ReadWrite_Note`
- [ ] `TinyLSM_Compaction_Strategies_Note`
- [ ] `TinyLSM_Manifest_WAL_Recovery_Note`
- [ ] `TabletServer_Request_Path_Index`
- [ ] `TabletServer_Read_Write_Path_Map`
- [ ] `RocksDB_LSM_Refresh.md`
- [ ] `RocksDB_Read_Write_Path_Comparison`
- [ ] `RocksDB_Open_Recovery_WAL_Note`
- [ ] `TokaDB_RocksDB_LSM_Integration_Review`
- [ ] `SO-ARM101_first_loop_report_v0.md` 或 `hardware_blocker_report.md`（P2）

## 对应周计划

- `2026-W27`：LSM in a Week / mini-lsm Week 1 入口 + TokaDB TabletServer request path index。
- `2026-W28`：mini-lsm block / SST / read-write path + TabletServer read/write path map + RocksDB read/write 对照。
- `2026-W29`：mini-lsm compaction strategies + RocksDB compaction / write stall / amplification deep dive。
- `2026-W30`：manifest / WAL / recovery + TokaDB/RocksDB open-recovery 对照 + 本月代码链路复盘。

## 月末自检

- [ ] 我能不用资料讲清 LSM write path：WAL -> memtable -> immutable memtable -> flush -> SST -> compaction。
- [ ] 我能不用资料讲清 LSM read path：memtable / immutable memtable / L0 / lower levels / Bloom/filter / block index / iterator。
- [ ] 我能解释 compaction 为什么同时影响 write amplification、read amplification、space amplification 和 tail latency。
- [ ] 我能画出 TokaDB TabletServer 一条 read/write/admin 请求进入 Replica/FSM/Engine 的主链路。
- [ ] 我能把 mini-lsm 的机制和 RocksDB/TokaDB 的生产实现差异讲清楚。
- [ ] 我能把机器人路线讲成长期 North Star，而不是当前短期转岗主战场。
