---
type: daily_note
date: 2026-06-10
linked_week: [[2026-W24|2026-W24]]
time_budget: "weekday: paper 20-40m + evening mainline 90m"
paper_slot: 20-40m
track: Robot Learning Full-Stack / SO-ARM101 / LeRobot
env: local Mac + SO-ARM101 hardware bring-up
---

# 2026-06-10 Daily Note

## 今日启动计划（start-my-day 2026-06-10 17:03 CST）

### 方向锚点

- 今天属于 `2026-W24`，本周唯一主线是 `SO-ARM101 + LeRobot` 实物首闭环启动。
- 昨天已经把 W24 路线收敛成 `最小 CV -> CLIP / BLIP-2 / LLaVA -> RT-2 / LingBot / SmolVLA -> LeRobot / SO-ARM101 bring-up`；今天不再扩新论文或 world model 主题。
- 昨日承接点非常明确：`SO-ARM101 到货优先`。如果硬件到货，阅读让位给开箱、清点、拍照、端口、电机 ID、校准入口；如果没到货，则完成 LeRobot paper/docs 和 bring-up checklist。
- 今日 paper override：无；`LLM_Learn/04_Papers/99_Overrides/2026-06-10.md` 不存在。

### 本周 / 今日目标

| 层级 | 目标 | 今天怎么推进 |
|---|---|---|
| 本周目标 | 把 `SO-ARM101 + LeRobot` 推进到可装、可记录、可失败分析的真实项目状态 | 今天优先做开箱清点和 E001 bring-up 记录 |
| 今日目标 | 让硬件事实替代继续泛读：确认到货、清点、拍照、准备端口/电机 ID/校准入口 | 到货就做实物；未到货就把 LeRobot 命令链路和 checklist 写完整 |
| 今日证据 | `BOM` 清点结果 + `E001 Hardware Bring-Up` 记录 + LeRobot paper/docs takeaway | 写回本 Daily、BOM、Experiment Log、LeRobot note |

### 今日最低完成线

- [ ] `SO-ARM101`：完成到货后第一小时清点，至少记录外包装、12 个舵机、2 个 Servo Driver Board、电源、USB-C 线、DC 线、固定夹、3D 打印件、螺丝和缺件情况。
- [ ] `E001 Hardware Bring-Up`：写下今天真实状态：是否到货、是否拍照、端口是否识别、电机 ID 是否开始配置、卡在哪一步、下一步是什么。
- [ ] `LeRobot`：完成 20-40m paper/docs scan，只回答 `teleop / record / replay / train / eval` 的命令链路和数据字段，不展开成完整源码阅读。

如果硬件未到，最低完成线改为：

- [ ] 把 `BOM` 的到货前准备全部过一遍，缺什么就标出来。
- [ ] 在 `E001 Hardware Bring-Up` 写出可直接执行的端口识别、电机 ID、校准命令清单。
- [ ] 在 LeRobot note 写一句 takeaway：LeRobot 如何把真实机器人硬件、dataset、policy、eval 串成闭环。

### 建议顺序

1. 先确认硬件是否到货：到货就立刻开箱清点；未到货就进入 LeRobot docs / checklist。
2. 做今日 paper slot：`LeRobot` 20-40m，只读论文摘要和官方 SO-101 / imitation learning 文档中的 `ports -> motor ID -> calibration -> teleop/record/replay`。
3. 晚间 90m 只围绕 E001：拍照、清点、端口、电机 ID、校准入口和失败记录。
4. 如果硬件进展顺利，再用 20m 快速扫 `ACT / ALOHA` 的 action chunk；否则不读 ACT。
5. 日终写清明天唯一主线：`calibration / teleop / record` 三者中最靠前的未完成项。

### 今日 Top 3

- [ ] `到货清点`：把 SO-ARM101 套件从“已下单”推进到“实物清单已核对 / 缺件已记录”。
- [ ] `E001 bring-up`：在实验日志里记录端口、命令、观察、失败和下一步。
- [ ] `LeRobot 主链路`：把 `assemble -> calibrate -> teleop -> record -> replay -> train/eval` 写成今天能执行的命令入口。

### 今日论文槽位

- 论文 / 材料：`LeRobot: An Open-Source Library for End-to-End Robot Learning` + LeRobot SO-101 官方文档
- arXiv ID：`2602.22818`
- arXiv URL：`https://arxiv.org/abs/2602.22818`
- PDF：`https://arxiv.org/pdf/2602.22818`
- 官方文档：
  - `https://huggingface.co/docs/lerobot/main/assemble_so101`
  - `https://huggingface.co/docs/lerobot/il_robots`
- 阅读模式：Project Must Read / Paper + Docs Scan（20-40m）
- 指定原因：今天的核心不是继续扩 VLA 论文，而是把 SO-ARM101 首闭环落到真实硬件和 LeRobot 命令链路；LeRobot 直接连接 `hardware -> teleop -> dataset -> replay/train -> eval`。
- 输出位置：
  - 本页 `今日论文 takeaway`
  - `/Users/bytedance/.openclaw/workspaces/llm-learner/LLM_Learn/04_Papers/40_Data_and_Eval/LeRobot/QUICK_READ.md`
  - `/Users/bytedance/.openclaw/workspaces/llm-learner/LLM_Learn/03_Projects/so-arm101-lerobot-first-loop/05_Experiment_Log.md`
  - `/Users/bytedance/.openclaw/workspaces/llm-learner/LLM_Learn/03_Projects/so-arm101-lerobot-first-loop/02_Budget_And_BOM.md`

### 今日时间切片

- [ ] Paper Slot（20-40m，白天已过则作为晚间 warm-up）：LeRobot paper/docs scan，只抓命令链路和 dataset/eval 字段。
- [ ] Evening Session 1（30-40m）：SO-ARM101 开箱清点、拍照、按 BOM 勾选缺件。
- [ ] Evening Session 2（40-50m）：尝试或准备 `ports -> motor ID -> calibration`；能跑就记录命令输出，不能跑就记录阻塞。
- [ ] Close-out（10m）：回写 E001、今日总结、明天唯一主线。
- [ ] 可选补位（20m）：硬件顺利后扫 ACT / ALOHA，只问 action chunk 和 teleop 数据，不展开训练。

### 对应文件或命令

文件：

- Daily：`/Users/bytedance/.openclaw/workspaces/llm-learner/LLM_Learn/01_DailyNotes/2026/2026-06/2026-06-10.md`
- BOM：`/Users/bytedance/.openclaw/workspaces/llm-learner/LLM_Learn/03_Projects/so-arm101-lerobot-first-loop/02_Budget_And_BOM.md`
- E001 实验日志：`/Users/bytedance/.openclaw/workspaces/llm-learner/LLM_Learn/03_Projects/so-arm101-lerobot-first-loop/05_Experiment_Log.md`
- LeRobot note：`/Users/bytedance/.openclaw/workspaces/llm-learner/LLM_Learn/04_Papers/40_Data_and_Eval/LeRobot/QUICK_READ.md`
- ACT note（可选）：`/Users/bytedance/.openclaw/workspaces/llm-learner/LLM_Learn/04_Papers/20_Robot_Learning/ACT/QUICK_READ.md`
- Data schema：`/Users/bytedance/.openclaw/workspaces/llm-learner/LLM_Learn/03_Projects/so-arm101-lerobot-first-loop/notes/data_schema.md`

命令入口（按官方文档替换实际端口；今天优先记录，不强行全部跑完）：

```bash
python lerobot/scripts/find_motors_bus_port.py
```

```bash
python lerobot/scripts/configure_motor.py \
  --port <motor_bus_port> \
  --brand feetech \
  --model sts3215 \
  --baudrate 1000000 \
  --ID <1-6>
```

```bash
python lerobot/scripts/control_robot.py \
  --robot.type=so101 \
  --robot.cameras='{}' \
  --control.type=calibrate \
  --control.arms='["main_follower"]'
```

```bash
python lerobot/scripts/control_robot.py \
  --robot.type=so101 \
  --robot.cameras='{}' \
  --control.type=calibrate \
  --control.arms='["main_leader"]'
```

后续 record / replay / train 入口先只读不强行推进：

```bash
lerobot-replay \
  --robot.type=so101_follower \
  --robot.port=<follower_port> \
  --robot.id=<follower_id> \
  --dataset.repo_id=<dataset_repo_id> \
  --dataset.episode=0
```

```bash
lerobot-train \
  --dataset.repo_id=<dataset_repo_id> \
  --policy.type=act \
  --output_dir=outputs/train/act_so101_test \
  --job_name=act_so101_test \
  --policy.device=<cuda_or_mps> \
  --wandb.enable=false
```

## 今日输入

- 文档 / 论文：
  - `LeRobot: An Open-Source Library for End-to-End Robot Learning`
  - LeRobot SO-101 assemble docs
  - LeRobot imitation learning on real robots docs
- 项目材料：
  - `SO-ARM101 + LeRobot` BOM
  - `E001 Hardware Bring-Up`
  - W24 Core Path Reading Pack

## 今日代码 / 实验任务

- [ ] 阅读材料 / 课程：LeRobot paper/docs 20-40m
- [ ] 跑实验 / 硬件：开箱清点；可行则进入 port / motor ID / calibration
- [ ] 写笔记 / 画图：更新 E001 和 BOM

## 今日实验记录

- 项目 / 主题：SO-ARM101 + LeRobot / E001 Hardware Bring-Up
- 环境：Mac 本地 + SO-ARM101 Pro leader/follower
- 参数 / 配置：
  - leader port：
  - follower port：
  - camera：
  - LeRobot version：
- 指标 / 观察点：
  - 是否到货：
  - 是否缺件：
  - 是否识别端口：
  - 是否完成 motor ID：
  - 是否进入 calibration：
- 观察：

## 今日系统记录

- 入口 / 章节 / 模块：LeRobot SO-101 assemble / imitation learning on real robots
- 主链路：`hardware -> ports -> motor ID -> calibration -> teleop -> record -> replay -> train/eval`
- 和本周目标的关系：这是 W24 从阅读转向真实 robot learning 闭环的关键入口。
- 今天最不懂的点：

## 今日完成情况

- 完成了什么：
- 没完成什么：
- 原因：

## 硬件阻塞后的等待期安排

- 当前状态：SO-ARM101 已到货并完成部分清点，但缺合适螺丝刀，且部分物料仍需继续核对；今天不强行开始机械装配。
- 决策：等待工具/配件期间可以读论文，但只读服务 `SO-ARM101 + LeRobot -> policy runtime` 的材料，不扩成泛 CV 或泛 VLA 专题。
- 当前节奏确认：
  - 今天 / 明天：子豪兄教程装配前章节 + LeRobot 数据主链路 + ACT quick scan。
  - 周四：有 2-3 小时，排成密集学习块，但仍围绕 SO-ARM101 / LeRobot；允许放入一个 LLM 收口块。
  - 周五：时间少，只做轻量学习、checklist 和 LLM 收口，不新增大主题。
  - 周日：集中搭建前几个章节，目标是把机械臂相关装配推进到真实硬件状态。
- 这两天的学习顺序：
  1. `LeRobot`：补齐 dataset / teleop / record / replay / train / eval 主链路，输出数据字段和命令链路。
  2. `RT-2`：收口 action-as-token、VLA 输入输出和 runtime 限制，输出 5-8 条接口笔记。
  3. `ACT / ALOHA`：只读 observation、action chunk、teleop 数据、eval / failure；这是 SO-ARM101 第一版 policy 最贴近的算法材料。
  4. `SmolVLA / LingBot-VLA`：只做 schema mapping，回答 observation/action/data/eval/deploy 怎么对上 LeRobot。
  5. `CV 基础`：只补 `ResNet / ViT / CLIP` 的最小视觉表示直觉，不读 AlexNet/VGG/GoogLeNet 全套历史线。
- 暂不进入：HoloMotion、Unitree locomotion、Isaac Lab、world model、Jetson/Thor 采购、VLA full fine-tune。

## 周四 2-3h 密集学习块草案

- 45m：子豪兄教程装配前路径：`准备舵机 / 舵机控制板接电源线 / 舵机中位校准并编号 / Mac 串口与校准章节`。
- 35m：LeRobot 数据主链路：`teleop -> record -> replay -> train ACT -> eval -> failure log`，只补字段和命令理解。
- 35m：ACT quick scan：`observation / action chunk / teleop data / eval / failure`。
- 25m：SmolVLA awareness：只看 `SO100/SO101 / LeRobot community data / action chunk / async inference`，不进训练。
- 30-45m：LLM 收口：tokenizer 或 nanoGPT 主链路二选一；只写总结，不新增论文。

如果时间不够，砍掉 SmolVLA，不砍教程、LeRobot 数据链路和 ACT。

## 灵感 / 问题

- 灵感：
- 明天需要验证的问题：

## 关联笔记

- [[00_North_Star]]
- [[03_Annual_Plan_2026]]
- [[2026-W24]]
- [[Core_Path_Reading_Pack_2026-W24]]

## 今日总结

### 今日实际完成

- `SO-ARM101` 已到货，并确认收到两盒核心物料：`3D printed parts only` 和 `Servo Motor Kit Pro`。
- 完成初步开箱与关键数量核对：leader / follower 3D 打印件各 12 pcs，看到 4 个 fixing clips；舵机数量符合预期：`C001 x1`、`C044 x2`、`C046 x3`、`C047 x6`。
- 补充采购 USB 摄像头、多功能螺丝刀、万用表，并确认暂不继续追加 Jetson、RealSense、3D 打印机等大件。
- 拉取并整理子豪兄 `LeRobot / SO-ARM101` 教程目录，明确首闭环顺序：`开箱清点 -> 准备舵机 -> 组装 -> 串口 -> calibration -> teleop -> record/replay -> ACT train/eval`。
- 将等待期安排收敛到 `SO-ARM101 + LeRobot -> policy runtime`，不扩成泛 CV / 泛 VLA 阅读。

### 未完成与原因

- 未完成完整 BOM 清点：控制板、线材、电源线、螺丝、全部 3D 打印件仍需逐项核对。
- 未开始机械装配、通电、端口识别、motor ID、calibration、teleop：主要原因是缺合适螺丝刀，且部分物料仍未确认；为避免滑丝、接错电源或 leader/follower 零件混放，今天不强行推进。
- `LeRobot` paper/docs 还未形成完整数据字段和命令链路笔记，`ACT / ALOHA` 也未完成 quick scan；今天的有效进展主要集中在硬件事实和教程路径整理。

### 今日学习证据

- `LLM_Learn/03_Projects/so-arm101-lerobot-first-loop/05_Experiment_Log.md`：`E001 Hardware Bring-Up` 记录到货、初步开箱、舵机数量、阻塞和下一步。
- `LLM_Learn/03_Projects/so-arm101-lerobot-first-loop/02_Budget_And_BOM.md`：记录 2026-06-10 补充采购和暂不购买项。
- `LLM_Learn/03_Projects/so-arm101-lerobot-first-loop/09_Primary_Tutorial_Zihao_AI.md`：整理子豪兄教程目录、裁剪后的执行顺序和待细读章节。
- `LLM_Learn/03_Projects/so-arm101-lerobot-first-loop/notes/data_schema.md`：记录第一阶段 camera plan 和 `observation.images / observation.state / action / task` 字段问题。

### 今日论文 takeaway

- `LeRobot` 对当前阶段的价值不是论文结论本身，而是把真实硬件、teleop、dataset、replay、train/eval 串成一条可执行闭环；明天应该继续把教程命令和数据字段落到 `E001`，而不是扩更多 VLA 论文。
- `ACT / ALOHA` 暂时只作为下一步 policy 视角：第一版要先拿到可靠 teleop / record 数据，再讨论 action chunking 和 imitation learning。

### 明天唯一主线

- `2026-06-11` 只推进 `SO-ARM101 E001 bring-up`：完成剩余物料清点和教程装配前章节；如果螺丝刀到位，再进入舵机准备 / 中位校准入口。

### 明天最低完成线

- 完成剩余 BOM 清点：控制板、线材、电源、螺丝、3D 打印件、摄像头 SKU 和缺件记录。
- 读完并摘出子豪兄教程中的 `开箱清点`、`准备舵机`、`舵机中位校准并编号`、`Mac 环境 / 串口 / 校准` 的可执行步骤。
- 更新 `E001 Hardware Bring-Up`：写清能否装配、能否通电、下一条命令是什么、当前 blocker 是什么。

### 明天承接点

- 从 `E001 Hardware Bring-Up` 的下一步继续：先分拣 leader / follower、核对 7.4V / 12V 舵机和供电线；工具未到前不通电、不强装。
