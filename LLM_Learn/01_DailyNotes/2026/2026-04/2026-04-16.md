---
type: daily_note
date: 2026-04-16
linked_week: [[2026-W16|Week 2026-W16 学习记录]]
time_budget: 1.5-2h
track: LLM Inference / Serving
gpu_env: V100
---

# 2026-04-16 Daily Note

## 今日锚点

- 今天属于哪一周：`2026-W16`
- 今日主题：`最后一轮完整过代码，并结合训练观测参数理解训练大改与稳定性`
- 今日硬产出：`1 份训练主流程代码级理解` + `1 份核心训练观测参数清单` + `1 份训练稳定性改法总结`
- 今日时间预算：`1.5-2h`

## 今日 Top 3

- [x] 从头到尾再过一轮当前 `makemore` / 对应训练代码，讲清 `data -> embedding / hidden -> logits -> loss -> backward -> update -> sample` 主链路
- [x] 重点看训练过程里的核心观测量：`loss`、`logits / prob` 分布、`activation`、`gradient`、`grad:data ratio / update:data ratio`、参数尺度变化
- [x] 最后总结：训练里哪些地方需要“大改”，分别在解决什么问题，以及如何提高训练稳定性

## 今日时间切片

- [ ] Session 1（35-45m）：完整顺一遍代码主流程，只抓模块职责、关键张量和训练循环
- [ ] Session 2（30-40m）：专门看训练观测与诊断信号，记录哪些现象对应初始化 / 激活 / 梯度 / 学习率问题
- [ ] Session 3（30-40m）：总结“训练大改怎么写”，压成自己的 checklist
- [ ] Session 4（可选 15-20m）：补一版 `训练稳定性 / 初始化 / 训炸原因` 笔记

## 今日输入

- 文档 / 论文：
- 源码 / PR：`karpathy/makemore` 当前训练代码
- 视频 / blog：`makemore` 对应讲解视频 / 笔记

## 今日代码 / 实验任务

- [ ] 跑环境 / 服务
- [x] 读源码
- [ ] 写脚本 / 改代码
- [ ] 跑 benchmark / profiling
- [x] 写笔记 / 画图

## 今日实验记录

- 模型：
- 框架：PyTorch
- GPU：V100
- 参数：
- 指标：
- 观察：

## 今日源码记录

- 入口函数 / 模块：训练脚本主循环、forward、loss 计算、参数更新、sampling
- 调度相关：训练 step 内各阶段的先后顺序与观测点插入位置
- cache / memory 相关：先不展开，当前只聚焦训练主链路与稳定性
- 今天最不懂的点：训练里哪些观测量最能提前暴露“要训炸了”或“初始化 / 学习率不对”

## 今日完成情况

- 完成了什么：完整过了一轮 `makemore` 当前训练代码，重新讲顺了 `dataset -> embedding -> MLP -> logits -> cross entropy -> backward -> update -> sample` 的主链路；重点梳理了 `block_size`、`Linear(fan_in, fan_out)`、`randn` 初始化、`retain_grad()`、`BatchNorm1d` 的作用边界；同时把训练诊断部分收成了 4 类核心观测：激活值分布、激活梯度分布、权重梯度分布、`update-to-data ratio`。
- 没完成什么：今天没有继续扩展到更系统的 `backprop` 推导，也没有继续推进后续新模型部分。
- 原因：今天目标是把当前训练代码与训练稳定性先收成闭环，不再横向扩材料，这样对后续继续学 `backprop` 和收尾 `makemore` 更稳。

## 灵感 / 问题

- 灵感：`makemore` 这类代码真正的价值，不只是“会跑”，而是通过激活、梯度和更新幅度这些观测量，开始建立对训练稳定性的判断框架。
- 明天需要验证的问题：把注意力转到 `backprop` 本身后，我能不能把链式法则、局部梯度、参数梯度和当前训练循环里的 `loss.backward()` 真正对应起来。
- 本周收尾目标：周日把 `makemore` 第一轮学习完整收口，至少做到可以不依赖视频讲清主链路与主要训练稳定性改法。

## 关联笔记

- [[2026-W16|Week 2026-W16 学习记录]]
- [[2026-04_月计划|2026-04 月计划]]

## 今日总结

- 今天最重要的收获：已经把这一版 `makemore` 训练代码的主流程、核心观测量和训练稳定性改法大体学清楚了，尤其是把“为什么看 activation / gradient / update ratio”这件事从零散印象收成了结构化理解。
- 今天最大的阻塞：`backprop` 的底层直觉和公式级理解还没有完全补齐，所以当前对一些梯度观测还属于“能解释现象，但还没彻底吃透推导”。
- 明天唯一主线：看 `backprop`，把链式法则、局部梯度、`retain_grad()`、`loss.backward()` 和当前训练代码真正对上。
- 周日目标：完成 `makemore` 第一轮学习收口。
