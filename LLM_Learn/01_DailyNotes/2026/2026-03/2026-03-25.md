---
type: daily_note
date: 2026-03-25
linked_week: [[Week 2026-W13 学习记录]]
time_budget: 1.5h
track: LLM Inference / Serving
gpu_env:
---

# 2026-03-25 Daily Note

## 今日锚点

- 今天属于哪一周：`2026-W13 / 2026-03`
- 今日主题：`承接昨天，把 micrograd 后半部分看完，并收口 backward / gradient / 参数更新`
- 今日硬产出：`看完 micrograd 后半部分` + `留下 3-5 条关键理解` + `写 1 版 5-8 句最小解释`
- 今日时间预算：`14:45-16:15 / 1.5h`

## 今日 Top 3

- [x] 看了约 `40m`，把 `micrograd` 里完整的 `backward` 实现过了一遍
- [x] 对 `backward` 在计算图上怎么执行有了更具体的实现层理解
- [ ] 剩余 `nn` 相关实现留到明天继续看完并完整 review

## 今日时间切片

- [x] Session 1（约 40m）：看 `micrograd` 后半部分中 `backward` 的完整实现
- [ ] Session 2：原计划回看实现并收口 `topo order / grad accumulation`，今天未完成
- [ ] Session 3：原计划写最小总结并留承接点，改为直接给明天留 review 主线

## 今日输入

- 文档 / 论文：`今天不新开论文`
- 源码 / PR：`只在卡住时回看 micrograd 对应实现`
- 视频 / blog：`Karpathy micrograd 后半部分`

## 今日代码 / 实验任务

- [x] 不跑新环境，不开系统实验
- [x] 继续承接 `micrograd`，把 `backward` 实现完整过了一遍
- [ ] 输出一版可复述的 `训练闭环 / backward` 解释
- [x] 给明天留了明确入口：看完 `nn` 实现后做一次完整 review

## 今日实验记录

- 模型：
- 框架：
- GPU：
- 参数：
- 指标：
- 观察：

## 今日源码记录

- 入口函数 / 模块：`Value.backward()` 以及相关局部操作的 `_backward` 闭包
- 调度相关：
- cache / memory 相关：
- 今天最不懂的点：`nn` 相关实现还没看完；`topological order`、梯度累加、参数更新链条还需要明天结合完整实现再统一收口`

## 今日完成情况

- 完成了什么：`今天虽然时间和精力都有限，但还是看了约 40 分钟；把 micrograd 里完整的 backward 实现过了一遍，对反向传播不再只是概念直觉，而是开始看到代码里怎么把计算图、局部导数和反向传播串起来`
- 没完成什么：`还没看完后半部分剩余的 nn 实现，也还没做一次从 Value 到神经网络封装的完整 review；今天原计划中的 5-8 句最小解释也还没正式写出来`
- 原因：`今天回来比较晚，也比较累，适合先保最核心的 backward 实现，不适合强行把剩余 nn 实现和总结一起硬吃完`

## 灵感 / 问题

- 灵感：`今天最有价值的不是推进时长，而是第一次把 backward 从“概念”推进到“代码里到底怎么做”这一步`
- 明天需要验证的问题：`nn 那层封装是怎么建立在 Value/autograd 之上的；topological order、梯度累加和参数更新能不能结合完整实现讲顺`

## 关联笔记

- [[LLM_Infra_Annual_Plan_2026]]
- [[LLM_Inference_Optimization_Roadmap_2026]]
- [[Week 2026-W13 学习记录]]

## 今日总结

- 今天最重要的收获：`今天真正推进的是对 backward 实现本身的理解。之前更多是“链式法则在计算图上传递”的概念直觉，今天开始看到代码里是如何为每个局部操作挂上 `_backward`，再按拓扑顺序把梯度反向传回去的。这个进展说明反向传播开始从抽象概念落到可读实现了。`
- 今天最大的阻塞：`今天时间和精力都不够，只能先保最关键的 backward 实现；后半部分剩余的 nn 实现还没看完，所以目前对“从 Value 到神经网络模块”这条完整链路还没有彻底收口。`
- 5-8 句阶段性总结：`今天虽然只看了大约 40 分钟，但主线没有断。我把 micrograd 里完整的 backward 实现过了一遍，开始从代码层面理解反向传播是怎么工作的。现在对 backward 的认识不再只是“梯度沿计算图反向传播”这句概念，而是开始看到每个局部操作如何定义自己的局部求导逻辑。随后再通过一次统一的 backward 调用，把这些局部导数沿图反向串起来。今天还没有把后半剩余的 nn 实现看完，所以理解暂时停在 autograd 主体实现这一层。明天的关键不是重头再看，而是把 nn 部分补完，然后把整套实现完整 review 一遍。完成之后，应该就能把 backward、参数更新和最小神经网络封装讲顺了。`
- 明天唯一主线：`用约 2h 看完剩余 nn 实现，然后从头完整 review micrograd 实现，彻底收口 backward -> 参数更新 -> nn 封装这条链路`
