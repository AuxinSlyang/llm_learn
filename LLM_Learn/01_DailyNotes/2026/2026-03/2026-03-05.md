---
type: daily_note
date: 2026-03-05
linked_week: [[Week 2026-W10 学习记录]]
time_budget: 1h
track: LLM Inference / Serving
gpu_env: V100
---

# 2026-03-05 Daily Note

## 今日锚点

- 今天属于哪一周：`2026-W10`
- 今日主题：`Debian12 重装 + V100 推理环境重建（新栈）`
- 今日硬产出：`驱动/版本确认` + `新 venv 可用` + `最小 generate 验收` + `Qwen chat once 验收`
- 今日时间预算：`以实际为准（重装日）`

## 今日 Top 3

- [x] 重装系统到 `Debian 12`，确认 GPU 驱动可用
- [x] 新建推理 venv（`cu124 + py311`），跑通最小 `generate()`
- [x] 跑通 `Qwen2.5-1.5B-Instruct` 的 `apply_chat_template + generate`（单轮）

## 迁移说明

原计划（`HF Text generation` 阅读与摘记、最小主流程回写、周日实验骨架）整体迁移到 `2026-03-06`，避免 03-05 与“重装日”混在一起。

## 系统重装与环境重建记录

### 系统 / 驱动
- OS：`Debian 12`
- GPU：`Tesla V100-SXM2-32GB`
- Driver：`550.54.15`
- CUDA（driver reported）：`12.4`
- Python：`3.11.2`

### Python venv（新栈）
- venv：`~/venvs/llm-cu124-py311`
- 验收：tiny 模型 `generate()` 可运行（`sshleifer/tiny-gpt2`）
- 验收：Qwen `apply_chat_template + generate` 单轮可运行

### 可复用命令（重装后）
```bash
# 1) 驱动确认
nvidia-smi
python3 --version

# 2) 新 venv
python3 -m venv ~/venvs/llm-cu124-py311
source ~/venvs/llm-cu124-py311/bin/activate
python -m pip install -U pip setuptools wheel

# 3) PyTorch + Transformers（cu124 / py311）
python -m pip install torch==2.6.0 torchvision==0.21.0 torchaudio==2.6.0 --index-url https://download.pytorch.org/whl/cu124
python -m pip install transformers==4.57.3 accelerate safetensors huggingface_hub sentencepiece

# 4) 验收
python -c "import torch; print(torch.__version__); print(torch.cuda.is_available()); print(torch.cuda.get_device_name(0) if torch.cuda.is_available() else 'no cuda')"
```

## 今日输入

- 文档 / 论文：`无（以重装与环境为主）`
- 源码 / PR：`暂不看源码`
- 视频 / blog：`无`

## 今日代码 / 实验任务

- [x] 跑环境 / 服务（最小 generate 验收）
- [ ] 读源码
- [x] 写脚本 / 改代码（`tiny-gpt2` / `qwen_chat_once.py`）
- [ ] 跑 benchmark / profiling
- [x] 写笔记 / 画图（重装记录）

## 今日实验记录

- 模型：`Qwen2.5-1.5B-Instruct` + `tiny-gpt2`（smoke）
- 框架：`PyTorch + transformers`
- GPU：`V100`
- 参数：`先保证可跑；Qwen 走 chat template 单轮`
- 指标：`torch.cuda.is_available()`、最小 generate、Qwen 单轮输出是否稳定
- 观察：`重装后整体进入“可控的现代栈”，后续学习不再被驱动/torch 版本卡住`

## 今日源码记录

- 入口函数 / 模块：`本日不展开源码`
- 调度相关：`本日不展开`
- cache / memory 相关：`今天先不展开，留到 03-06 / 03-07`
- 今天最不懂的点：`chat template 和纯文本 prompt 在 chat 模型上的行为差异（迁移到 03-06 继续）`

## 今日完成情况

- 完成了什么：
  - 系统重装到 Debian12，驱动升级到 `550.54.15`
  - Python 升级到 `3.11.2`，新建 `cu124` venv
  - 跑通最小 `generate()`（tiny 模型）与 Qwen chat once
- 没完成什么：
  - `HF Text generation` 摘记 / 最小主流程回写 / 周日实验骨架（已迁移到 03-06）
- 原因：
  - 03-05 被系统重装与新环境验收占满，先把不确定性清零更值钱

## 灵感 / 问题

- 灵感：`把系统和依赖一次性升级到可控区间，后续学习才不会被环境反复打断`
- 明天需要验证的问题：`回到输入理解：HF generate/chat template 的最小链路是否能写成 3-5 条可复用笔记`

## 关联笔记

- [[LLM_Infra_Annual_Plan_2026]]
- [[LLM_Inference_Optimization_Roadmap_2026]]
- [[Week 2026-W10 学习记录]]

## 今日总结

- 今天最重要的收获：
- 今天最大的阻塞：
- 明天唯一主线：`回到输入理解：HF Text generation + 最小主流程 + 周日实验骨架（并保持 Karpathy/KV cache 主线）`
