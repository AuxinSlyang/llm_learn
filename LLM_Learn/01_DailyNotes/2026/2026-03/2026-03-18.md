---
type: daily_note
date: 2026-03-18
linked_week: [[Week 2026-W12 学习记录]]
time_budget: 4h（含 03-17 顺延补位）
track: LLM Inference / Serving
gpu_env: V100（今天不依赖）
---

# 2026-03-18 Daily Note

## 今日锚点

- 今天属于哪一周：`2026-W12 / 2026-03 / Week 3`
- 今日主题：`把 03-17 的轻输入和 03-18 的完整晚间时间拼成一次 4h 学习块`
- 今日硬产出：`makemore 第一段核心概念笔记` + `训练闭环 5-8 句版本` + `训练 vs 推理一页草稿`
- 今日时间预算：`今晚约 4h，可从 18:30 后展开`

## 今日 Top 3

- [ ] 看完 `makemore` 第一个 clip 的当前主线部分，补回 03-17 未完成内容
- [ ] 用自己的话写清 `Bigram / logits / cross entropy / loss.backward() / optimizer.step()`
- [ ] 写出 `训练闭环 5-8 句版本`，并起一版 `训练 vs 推理` 草稿

## 今日时间切片

- [ ] Session 1（45-60m）：回接昨晚读的文档和 `makemore`，看完当前该看的核心段落，只盯 `Bigram -> logits -> loss`
- [ ] Session 2（45-60m）：写 `Bigram / logits / cross entropy` 各 `2-3` 句自己的解释
- [ ] Session 3（45-60m）：写 `loss.backward() / optimizer.step() / generate` 各 `2-3` 句解释，并压出 `forward -> loss -> backward -> step` 的 `5-8` 句版本
- [ ] Session 4（40-60m）：整理成一页 `训练 vs 推理` 草稿，最后补 `1` 条今天最卡的问题

## 今日输入

- 文档 / 论文：`先回接 03-17 昨晚读过的那篇文档，不新开材料`
- 源码 / PR：`makemore / BigramLanguageModel（按需对照）`
- 视频 / blog：`Karpathy makemore 第一个 clip（继续作为主输入）`

## 今日代码 / 实验任务

- [ ] 不跑环境 / 服务
- [ ] 只在卡住时读 `makemore` 对应代码
- [ ] 不写新脚本 / 不切 benchmark / profiling
- [ ] 写笔记：`Bigram / cross entropy / backward / optimizer.step / generate`
- [ ] 写笔记：`训练 vs 推理` 一页草稿

## 今日实验记录

- 模型：`makemore / BigramLanguageModel`
- 框架：`PyTorch`
- GPU：`V100（今天不依赖）`
- 参数：`无`
- 指标：`今天只看理解正确性和可复述性`
- 观察：`今天目标不是“多看”，而是把最小训练闭环讲清`

## 今日源码记录

- 入口函数 / 模块：`从 token -> logits -> loss -> backward -> optimizer.step -> generate 的最小路径`
- 调度相关：
- cache / memory 相关：
- 今天最不懂的点：`cross entropy` 和 `generate` 各自为什么这样设计

## 今日完成情况

- 完成了什么：
- 没完成什么：
- 原因：

## 灵感 / 问题

- 灵感：`把两天拼成一个完整学习块，比每天都勉强开新任务更稳`
- 明天需要验证的问题：`训练和推理在代码路径上到底从哪一步开始分叉`

## 关联笔记

- [[LLM_Infra_Annual_Plan_2026]]
- [[LLM_Inference_Optimization_Roadmap_2026]]
- [[Week 2026-W12 学习记录]]

## 今日总结

- 今天最重要的收获：
- 今天最大的阻塞：
- 明天唯一主线：`如果今晚收口顺利，明天开始进入 attention 最小链路`

## 03-17 + 03-18 拼接版执行清单

### 最低完成线

- [ ] 看完 `makemore` 当前主线需要的核心段落
- [ ] 写出 `Bigram / logits / cross entropy / loss.backward() / optimizer.step()` 各 `2-3` 句解释
- [ ] 写出 `forward -> loss -> backward -> optimizer.step` 的 `5-8` 句版本
- [ ] 起一版 `训练 vs 推理` 草稿

### 建议执行顺序

- [ ] 先用 `10-15m` 回想昨晚那篇文档留下了什么，再进入 `makemore`
- [ ] 先收 `Bigram / logits / cross entropy`，再收 `backward / optimizer.step / generate`
- [ ] 最后统一把碎笔记压成一页，不边看边追求完美

### 今晚不要做

- [ ] 不开新文章 / 新视频
- [ ] 不切 `KV cache / TTFT / TPOT`
- [ ] 不做环境、benchmark、profiling
