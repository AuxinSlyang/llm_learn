---
type: daily_note
date: 2026-03-03
linked_week: [[Week 2026-W10 学习记录]]
time_budget: 2h
track: LLM Inference / Serving
gpu_env: V100
---


# 2026-03-03 Daily Note

## 今日锚点

- 今天属于哪一周：`2026-W10`
- 今日主题：`Week 1 / PyTorch 安装 + CUDA smoke test + 最小 transformers 准备`
- 今日硬产出：`原计划未完成，整体顺延到 2026-03-04`
- 今日时间预算：`工作日 2h（实际未执行）`

## 状态更正

- [x] 2026-03-03 因身体不适，没有执行原定远程环境与阅读任务
- [x] 本页保留原计划，作为 2026-03-04 的顺延任务来源
- [x] 本周主线不变，仍然是：`PyTorch -> CUDA smoke test -> transformers 最小加载 -> 最小阅读笔记`

## 今日 Top 3

- [ ] 在 `llm-infer` 环境中安装与当前驱动兼容的 `PyTorch`
- [ ] 跑通 `torch.cuda.is_available()`、GPU tensor、矩阵乘法、`torch.cuda.get_device_name(0)` smoke test
- [ ] 确定本周主模型为 `Qwen2.5-1.5B-Instruct`，安装 `transformers` / `accelerate` 并尝试一次最小加载

## 今日时间切片

- [ ] Session 1（50-60m）：激活 `llm-infer`，安装 `PyTorch`，记录版本与安装命令
- [ ] Session 2（50-60m）：跑 CUDA smoke test，安装 `transformers`，尝试最小 tokenizer / 模型加载
- [ ] Session 3（周末可选）：把环境盘点、安装命令、报错整理进 `V100 环境说明文档`
- [ ] Session 4（周末可选）：如果模型加载成功，补 1 个最小 prompt，为周五生成做准备

## 今日详细执行步骤

### Session 1
- [ ] `source ~/venvs/llm-infer/bin/activate`
- [ ] 执行 `python -V`、`pip -V`
- [ ] 执行 `pip install --upgrade pip`
- [ ] 优先尝试安装：`torch==1.13.1+cu116`
- [ ] 记录 `torch.__version__`、`torch.version.cuda`
- [ ] 如果安装失败，记下完整报错，不在今天发散到 `vLLM`

### Session 2
- [ ] 验证 `import torch`
- [ ] 验证 `torch.cuda.is_available()`
- [ ] 验证 GPU tensor 创建与一次矩阵乘法
- [ ] 验证 `torch.cuda.get_device_name(0)` 输出正常
- [ ] 安装 `transformers`、`accelerate`、`sentencepiece`
- [ ] 确定本周主模型优先为 `Qwen2.5-1.5B-Instruct`
- [ ] 尝试一次最小 tokenizer / 模型加载；今天不强求完整生成

## 今晚阅读清单

- [ ] 先看 Hugging Face `Text generation` 文档 `20-30m`  
  今晚只看：模型加载、tokenizer、`generate()` 最小示例
- [ ] 如果还有精力，再看 Karpathy 视频前 `30-40m`  
  今晚只抓：`token`、`embedding`、`logits`
- [ ] 不看 `PagedAttention`、`FlashAttention`、`speculative decoding`  
  这些留到最小推理闭环跑通后再进

## 今晚看到什么算完成

- [ ] 你能写出一版最小生成脚本骨架：`load tokenizer -> load model -> tokenize -> generate -> decode`
- [ ] 你能用自己的话写 `2` 句话：
  - [ ] `token` 是什么
  - [ ] `logits` 是什么
- [ ] 你能写 `1` 句话说明：`prefill` 和 `decode` 为什么后面会影响 serving 性能

## 今日建议执行命令

```bash
source ~/venvs/llm-infer/bin/activate
python -V
pip -V
pip install --upgrade pip
pip install torch==1.13.1+cu116 torchvision==0.14.1+cu116 torchaudio==0.13.1 --extra-index-url https://download.pytorch.org/whl/cu116
python -c "import torch; print(torch.__version__); print(torch.version.cuda); print(torch.cuda.is_available()); print(torch.cuda.get_device_name(0) if torch.cuda.is_available() else 'no cuda')"
python - <<'PY'
import torch
x = torch.randn(1024, 1024, device='cuda')
y = torch.randn(1024, 1024, device='cuda')
z = x @ y
print(z.shape, z.device, z.dtype)
PY
pip install transformers accelerate sentencepiece
```

## 今日输入

- 文档 / 论文：`无实际输入，原计划顺延`
- 源码 / PR：
- 视频 / blog：`无实际输入，原计划顺延`

## 今日代码 / 实验任务

- [ ] 跑环境 / 服务
- [ ] 读源码
- [ ] 写脚本 / 改代码
- [ ] 跑 benchmark / profiling
- [ ] 写笔记 / 画图

## 今日实验记录

- 模型：`Qwen2.5-1.5B-Instruct`（优先） / `Llama-3.2-1B-Instruct`（备选）
- 框架：`PyTorch + transformers`
- GPU：`V100`
- 参数：`先不调参，只验证环境可用性`
- 指标：`torch.cuda.is_available()`、GPU tensor / matmul 是否正常、模型是否能开始加载
- 观察：`无实际执行，任务顺延至 2026-03-04`

## 今日源码记录

- 入口函数 / 模块：
- 调度相关：
- cache / memory 相关：
- 今天最不懂的点：

## 今晚阅读摘记

- Hugging Face 文档 3 个要点：`未阅读`
- Karpathy 视频 3 个要点：`未观看`
- 我现在能讲清楚的 2 个概念：`待补`
- 我还讲不清楚的 1 个点：`待补`

## 今日完成情况

- 完成了什么：
  - 因身体不适，及时停止了原定实验任务，没有硬顶执行。
  - 明确把 03-03 的任务整体顺延到 03-04，不额外发散新的主题。
- 没完成什么：
  - 没有安装 `PyTorch`。
  - 没有跑 `CUDA smoke test`。
  - 没有安装 `transformers` 或做最小模型加载。
  - 没有完成 Hugging Face / Karpathy 的最小阅读任务。
- 原因：
  - 身体不适，实际可用精力不足，不适合做远程环境排错和模型加载这类连续性任务。

## 灵感 / 问题

- 灵感：`当身体状态不好时，优先保住周主线，不做额外扩项`
- 明天需要验证的问题：`03-04 是否能在 4h 内补完 PyTorch、smoke test、最小加载和最小阅读笔记`

## 关联笔记

- [[LLM_Infra_Annual_Plan_2026]]
- [[LLM_Inference_Optimization_Roadmap_2026]]
- [[Week 2026-W10 学习记录]]

## 今日总结

- 今天最重要的收获：
  - 本周优先级仍然清楚，延后的是执行，不是方向。
- 今天最大的阻塞：
  - 身体不适导致没有进入实验状态。
- 明天唯一主线：`补完 03-03 的环境与最小推理任务，并开始 03-04 的最小阅读与记录`
