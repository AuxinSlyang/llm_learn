---
type: daily_note
date: 2026-03-16
linked_week: [[Week 2026-W12 学习记录]]
time_budget: 2h
track: LLM Inference / Serving
gpu_env: V100
---

# 2026-03-16 Daily Note

## 今日锚点

- 今天属于哪一周：`2026-W12 / 2026-03 / Week 3`
- 今日主题：`makemore Week 1 Day 1：先吃下前 60m，建立 token -> embedding -> logits -> next token 的最小链路`
- 今日硬产出：`embedding/logits 各 2-3 句解释` + `forward -> loss -> backward -> step 最小闭环`
- 今日时间预算：`工作日 2h / 周末 5h`

## 今日 Top 3

- [ ] 看 `makemore` 前 `50-60m`，只抓 `token / embedding / logits / next-token prediction`
- [ ] 写出 `embedding` 和 `logits` 各 `2-3` 句自己的解释
- [ ] 写出 `forward -> loss -> backward -> optimizer.step` 的最小训练闭环，不追细节公式

## 今日时间切片

- [ ] Session 1（50-60m）：看 `makemore` 前半，先记 `token -> embedding -> logits -> next token`
- [ ] Session 2（25-30m）：整理 `embedding / logits`，每个概念写 `2-3` 句自己的话
- [ ] Session 3（25-30m）：写 `forward -> loss -> backward -> optimizer.step` 最小闭环
- [ ] Session 4（周末可选）：如果今天还有精力，再补 `cross entropy` 一句话解释

## 今日输入

- 文档 / 论文：`无新增，今天不并行开论文`
- 源码 / PR：`makemore / BigramLanguageModel（按需回看）`
- 视频 / blog：`Karpathy makemore（今天主输入，目标前 50-60m）`

## 今日代码 / 实验任务

- [ ] 不折腾环境，不跑 benchmark
- [ ] 只在卡住时回看 `makemore` 对应代码片段
- [ ] 写最小笔记，不要求今天补完整图
- [ ] 把可复述内容沉淀到今天这页，方便周末收口

## 今日实验记录

- 模型：`makemore / bigram 到最小神经网络理解`
- 框架：`PyTorch`
- GPU：`V100（今天不依赖）`
- 参数：`无`
- 指标：`今天只看理解正确性`
- 观察：`先补模型最小链路，不切 serving 细节`

## 今日源码记录

- 入口函数 / 模块：`makemore` 中从 token 到 logits 的数据流
- 调度相关：`今天不展开`
- cache / memory 相关：`今天不展开`
- 今天最不懂的点：

## 今日执行 TODO

### 最低完成线

- [ ] 看完 `makemore` 前 `50-60m`
- [ ] 留下至少 `6` 条自己的笔记，而不是只留时间戳
- [ ] 能用自己的话讲清 `embedding` 和 `logits`
- [ ] 能写出 `forward -> loss -> backward -> optimizer.step` 的最小流程

### P0：今天先做完

- [ ] 视频输入：`makemore` 前半，重点盯 `token / embedding / logits / next-token prediction`
- [ ] 概念 1：`embedding` 是什么，为什么它不是手工特征
- [ ] 概念 2：`logits` 是什么，它和概率是什么关系
- [ ] 训练闭环：写 `forward -> loss -> backward -> optimizer.step` 各自做什么
- [ ] 输出：压缩成 `5-8` 句今天可复述版本

### P1：如果还有 15-20 分钟再做

- [ ] 给 `cross entropy` 补一行：它在衡量什么
- [ ] 写一行：`next-token prediction` 为什么能变成语言模型
- [ ] 记下 1 个今天最卡的问题，留给周二继续处理

### 可直接填写的答案骨架

`embedding`：
- [ ] 它做的事是：
- [ ] 它为什么需要学出来：

`logits`：
- [ ] 它本质上是：
- [ ] 它和概率的关系是：

`训练闭环`：
- [ ] `forward` 在做什么：
- [ ] `loss` 在做什么：
- [ ] `backward` 在做什么：
- [ ] `optimizer.step` 在做什么：

## 今日完成情况

- 完成了什么：
  - 已看 `makemore` 第一个 clip 约 `40m`
  - 初步理解了 `Bigram` 的基本含义：当前 token 预测下一个 token 的最小语言模型视角
  - 主线没有跑偏，仍在补 `token -> embedding -> logits -> next-token prediction` 这条最小链路
- 没完成什么：
  - 还没看完今天原计划的前 `50-60m`
  - 还没把 `embedding / logits / forward -> loss -> backward -> optimizer.step` 系统写成自己的话
- 原因：
  - 今天先把理解建立起来，输出整理还没来得及完全落笔

## 灵感 / 问题

- 灵感：
- 明天需要验证的问题：

## 关联笔记

- [[LLM_Infra_Annual_Plan_2026]]
- [[LLM_Inference_Optimization_Roadmap_2026]]
- [[Week 2026-W12 学习记录]]

## 今日总结

- 今天最重要的收获：`对 Bigram 有了第一层直觉，开始把语言模型理解成“根据当前上下文预测下一个 token”的问题`
- 今天最大的阻塞：`还停留在视频输入阶段，自己的结构化输出还不够`
- 明天唯一主线：`完整看完这个 clip，并把 Bigram / token -> logits / 训练闭环 用自己的话列清楚吃透`

## 晚间补记

- 实际进展：今天是 `2026-03-16`，已完成 `makemore` 第一个 clip 约 `40m`
- 当前理解：`Bigram` 可以先粗理解为“只利用当前 token（或最小上下文）去预测下一个 token”的最小语言模型起点
- 明天计划：
  - 看完这个 clip 的剩余部分
  - 写出 `Bigram`、`embedding`、`logits` 各自的最小解释
  - 写出 `forward -> loss -> backward -> optimizer.step` 最小训练闭环
  - 留 `5-8` 句自己的总结，避免只留链接和时间戳
