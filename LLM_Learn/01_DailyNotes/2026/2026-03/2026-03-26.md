---
type: daily_note
date: 2026-03-26
linked_week: [[Week 2026-W13 学习记录]]
time_budget: 2h
track: LLM Inference / Serving
gpu_env:
---

# 2026-03-26 Daily Note

## 今日锚点

- 今天属于哪一周：`2026-W13 / 2026-03`
- 今日主题：`承接昨天，先看完 micrograd 的 nn 部分，再把 backward -> 参数更新 -> nn 封装完整串起来`
- 今日硬产出：`看完 micrograd 剩余 nn 实现` + `写 1 版 5-8 句最小解释` + `留下 3-5 条关键理解`
- 今日时间预算：`工作日 2h`

## 今日 Top 3

- [x] 看完 `micrograd` 剩余 `nn` 实现，确认 `Neuron / Layer / MLP` 是怎么建立在 `Value` 之上的
- [x] 从 `Value.backward()` 回到参数更新链路，建立了对 `backward -> grad accumulation -> parameter update` 的整体理解
- [x] 写出一版阶段性最小总结；代码细节留到后天 `2h` 完整 review

## 今日时间切片

- [x] Session 1（50-60m）：看完 `micrograd` 后半部分剩余 `nn` 实现，只记结构和职责，不抠所有细节
- [x] Session 2（40-50m）：从头 review 了一遍 `Value`、`backward`、参数集合、参数更新链路，但没有深抠所有代码细节
- [x] Session 3（20-30m）：输出阶段性最小总结，并决定把代码向完整 review 放到后天 `2h`

## 今日输入

- 文档 / 论文：`今天不新开论文`
- 源码 / PR：`micrograd` 对应实现
- 视频 / blog：`Karpathy micrograd 后半部分`

## 今日代码 / 实验任务

- [x] 不跑新环境，不开系统实验
- [x] 读完 `micrograd` 剩余 `nn` 实现
- [x] 回看 `Value.backward()` 到参数更新的完整链路
- [x] 写出阶段性最小解释
- [ ] 周六用 `2h` 做代码向完整 review，再决定是否补更细的链路笔记

## 今日实验记录

- 模型：
- 框架：
- GPU：
- 参数：
- 指标：
- 观察：

## 今日源码记录

- 入口函数 / 模块：`Value.backward()`、`Neuron`、`Layer`、`MLP`
- 调度相关：
- cache / memory 相关：
- 今天最不懂的点：`参数更新到底如何和 loss / backward / zero_grad 串成最小训练闭环`

## 今日完成情况

- 完成了什么：`今天把 micrograd 整体看完了，对 backward、梯度传播、参数更新和 MLP 封装之间的关系已经建立了比较清楚的整体理解；虽然没有把每一行代码都细抠，但主链路已经通了。`
- 没完成什么：`还没有把所有代码细节逐行吃透，也还没有做一次严格的代码级完整 review。`
- 原因：`今天优先保证“先看完并建立整体图景”，而不是在单个实现细节里停太久；代码级细抠更适合放到后天专门留出的 2 小时里完成。`

## 灵感 / 问题

- 灵感：`先把整体链路看通，再回头做代码级 review，比一边看一边深抠更适合当前阶段。`
- 明天需要验证的问题：`周六的代码 review 需要重点盯住哪些实现点；周日用 6h 继续推进 makemore 剩余内容，争取在本周结束前把 makemore + micrograd 都形成阶段性收口。`

## 关联笔记

- [[LLM_Infra_Annual_Plan_2026]]
- [[LLM_Inference_Optimization_Roadmap_2026]]
- [[Week 2026-W13 学习记录]]

## 今日总结

- 今天最重要的收获：`今天把 micrograd 整体看完之后，已经能把它放回训练闭环里理解了：backward 的本质是链式法则在计算图上的实现，MLP 本质上是多层全连接神经元的组合，而训练就是定义损失后沿梯度反方向不断更新参数、逐步逼近更优解的过程。`
- 今天最大的阻塞：`代码细节还没有完全吃透，尤其是一些具体实现上的小地方还来不及逐行推敲；这部分不适合今天硬抠，放到后天专门的 2 小时 review 更合适。`
- 5-8 句阶段性总结：`今天把 micrograd 整体看完了，已经对它在训练闭环里的位置有了比较清楚的理解。backward 的本质是链式法则在 DAG 上的实现，代码里则体现为按拓扑顺序反向传播并累计梯度。MLP 本质上是多层全连接神经元的组合，每层输出会成为下一层的输入，因此一旦层大小确定，整体连接关系和参数形状也基本确定。训练的过程则是先定义损失函数，再通过梯度下降这类迭代式方法不断更新参数，让损失往更小的方向移动。因为模型参数很多、函数复杂，我们通常不直接解析求最优解，而是用数值优化逐步逼近较优结果。在这个过程中，学习率也就是步长非常关键，太小会收敛慢，太大则可能震荡甚至发散。今天的收获主要是把整体链路看通了，代码级细节留到后天完整 review。`
- 明天唯一主线：`明天不扩新主题；周六用 2h 完整 review micrograd 代码链路，周日用 6h 推进 makemore 剩余内容并做本周阶段性收口`
